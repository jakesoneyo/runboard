DATABASE_URL="postgresql://user:password@localhost:5432/runboard?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/runboard?schema=public"
JWT_SECRET="change-me-in-.env"
JWT_REFRESH_SECRET="change-me-too"
PORT=3000
# 초대 링크 조립용(선택) — 없으면 상대 경로만 반환한다. 프론트 배포 도메인(Vercel)로 설정.
FRONTEND_URL=""
