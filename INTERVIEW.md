# INTERVIEW — Runboard 면접 대비 (1부)

> 이 프로젝트 기반 **예상 질문 + 모범 답안**. 개념 설명이 필요하면 [STUDY.md](./STUDY.md) · [STUDY-2.md](./STUDY-2.md)를 먼저 본다.
>
> **사용법**: 답안을 외우지 말고 **구조**를 외운다. 각 답안은 `핵심 한 줄 → 근거 → 트레이드오프` 순서다.
> ⚠️ 표시는 **틀리게 말하면 감점되는 지점**이다.
>
> **1부(이 문서)**: 오프닝 · 프로젝트 전반 · 기술 선택 · 멀티테넌시 · RBAC/인증
> **[2부(INTERVIEW-2.md)](./INTERVIEW-2.md)**: 실시간 · 감사로그 · DB/성능 · 트러블슈팅 STAR · 배포 · 테스트 · 프론트 · 압박 질문 · 한계 · 역질문

---

## 🎯 컷시트 (면접 당일 5분 훑어보기)

- **30초 소개**: 여러 조직이 하나의 DB를 공유하면서도 서로의 데이터를 절대 볼 수 없어야 하는 멀티테넌트 구조가 핵심이고, 여러 명이 같은 테스트 실행을 동시에 진행하면 WebSocket으로 실시간 동기화된다. 격리를 애플리케이션 한 곳에만 맡기지 않고 가드·Prisma Client Extension·DB 복합 FK 세 계층으로 구현한 게 가장 신경 쓴 부분이다. (0장 오프닝 스크립트)
- **가장 어려웠던 문제**: "실수해도 안전한 구조를 만드는 것"이었다 — 쿼리 100개 중 1개를 빠뜨리면 격리가 없는 것과 같고, 그 1개는 코드 리뷰로 잡히지 않는다. "조심하기"가 아니라 "조심할 필요가 없게 만들기"로 방향을 잡았다. (Q1-4)
- **대표 설계 질문**: Role을 JWT에 넣지 않은 이유 — 서버가 무효화할 수 없는 토큰에 권한을 박으면 강등·추방이 토큰 만료(15분)까지 반영되지 않는다. 요청 시점 Membership 조회로 대체했고, 대가는 요청당 인덱스 조회 1회다. (Q4-2)
- **아쉬운 점**: 수평 확장 경험이 없다 — 인스턴스 1대 전제로 설계했고, 실제로 여러 인스턴스를 띄워 룸 브로드캐스트가 깨지는 걸 겪어본 적은 없다. (INTERVIEW-2.md Q13-7)
- **데모 계정 압박 대비**: 우회한 것은 딱 하나, "이메일 형식 검증"뿐이고 인증 절차 자체는 전혀 우회하지 않는다. (Q5-7)

---

## 목차 (1부)

0. [오프닝 스크립트](#0-오프닝-스크립트)
1. [프로젝트 전반](#1-프로젝트-전반)
2. [기술 선택 이유](#2-기술-선택-이유)
3. [멀티테넌시 — 이 프로젝트의 심장](#3-멀티테넌시--이-프로젝트의-심장)
4. [RBAC / 인가](#4-rbac--인가)
5. [인증 (JWT · refresh 회전 · 재사용 탐지)](#5-인증-jwt--refresh-회전--재사용-탐지)

---

## 0. 오프닝 스크립트

### 30초 버전 (자기소개 중 언급)

> "Runboard는 QA팀용 테스트 케이스·실행 관리 SaaS입니다. **여러 조직이 하나의 DB를 공유하면서도 서로의 데이터를 절대 볼 수 없어야 하는 멀티테넌트 구조**가 핵심이고, 여러 명이 같은 테스트 실행을 동시에 진행하면 **WebSocket으로 실시간 동기화**됩니다. 격리를 애플리케이션 한 곳에만 맡기지 않고 **가드 · Prisma Client Extension · DB 복합 FK 세 계층**으로 구현한 게 이 프로젝트에서 가장 신경 쓴 부분입니다."

### 1분 버전

> "QA팀이 스프레드시트로 테스트를 관리할 때 생기는 문제를 푸는 서비스입니다. 특히 **외주·계약직이 섞인 팀에서 다른 고객사 데이터가 절대 새면 안 된다**는 요구가 이 프로젝트의 출발점입니다.
>
> 기술적으로 세 가지에 집중했습니다.
>
> **첫째, 멀티테넌시를 3계층으로 강제했습니다.** 1계층은 가드가 매 요청 Membership을 조회해 조직 소속을 확인하고, 아니면 403이 아니라 **404**를 줍니다. 조직의 존재 자체를 숨기기 위해서입니다. 2계층은 **Prisma Client Extension**이 모든 테넌트 쿼리에 `organizationId`를 자동 주입합니다. 서비스 코드에는 손으로 쓴 조직 필터가 없고, 컨텍스트가 없으면 조용히 전체 조회되는 게 아니라 **예외를 던집니다.** 3계층은 DB 복합 외래키라서, 앱이 뚫려도 다른 조직의 부모를 참조하는 행은 DB가 거부합니다.
>
> **둘째, 권한을 토큰에 담지 않았습니다.** JWT는 서버가 무효화할 수 없어서, 역할을 토큰에 박으면 **강등이나 추방이 토큰 만료까지 반영되지 않습니다.** 그래서 토큰에는 '누구인가'만 담고 '이 조직에서 무엇을 할 수 있는가'는 요청 시점 Membership 조회로 결정합니다. 대가는 요청당 인덱스 조회 1회입니다.
>
> **셋째, 실시간의 위험 두 가지를 구조로 막았습니다.** 쓰기는 REST만 쓰고 소켓은 브로드캐스트 전용입니다 — 검증·인가·트랜잭션·감사로그 경로를 하나로 유지하기 위해서입니다. 그리고 **emit은 트랜잭션 커밋 이후에만** 나갑니다. 롤백된 상태를 다른 사용자에게 방송하지 않기 위해서고, 이걸 Testcontainers 통합테스트로 실제 증명했습니다."

### 3분 버전 (기술 심화 요청 시)

위 1분에 다음을 덧붙인다:

> "구현하면서 실제로 막힌 것들이 있습니다.
>
> 하나는 **리프레시 토큰 재사용 탐지**입니다. 폐기된 토큰이 다시 오면 탈취로 보고 그 계열 전체를 폐기하는데, 처음엔 폐기 UPDATE와 401 throw를 **같은 트랜잭션**에 넣었습니다. 그랬더니 throw가 롤백을 일으켜서 **폐기가 없던 일이 됐습니다.** 401은 나가는데 실제로는 아무것도 폐기되지 않는, 보안 기능이 동작하는 척만 하는 상태였습니다. 폐기를 그 자체로 완결된 작은 트랜잭션으로 분리하고 커밋된 뒤에 throw하도록 고쳤습니다. 여기서 **트랜잭션 경계는 '묶을 수 있는 범위'가 아니라 '함께 성공하거나 함께 실패해야 하는 범위'** 라는 걸 배웠습니다.
>
> 배포 전 검수에서는 블로커 5건이 나왔습니다. 그중 가장 인상적이었던 건 **같은 결과를 두 번 기록하면 카운터가 이중 증가**하던 버그입니다. 카운터 이동을 `data[prev] = {decrement:1}; data[next] = {increment:1}`로 짰는데, 두 필드가 같으면 **객체 키가 덮어써져서 감소가 사라졌습니다.** 기존 테스트는 FAIL→PASS처럼 서로 다른 필드 전이만 검증해서 이 버그를 통과시켰습니다. **'테스트가 그린인 것과 버그가 없는 것은 다르다'** 는 걸 정확히 보여준 사례라, 지금은 delta 누적 방식으로 고치고 자기 전이 회귀 테스트를 추가했습니다.
>
> 나머지 블로커들도 공통점이 있었는데, 전부 **경계**에서 났습니다 — 코드와 배포 매니페스트의 경계(`DIRECT_URL` 누락으로 컨테이너 크래시), 프로덕션 환경변수 조합에서만 발현되는 문제(CORS 전체 허용), 백엔드와 프론트의 경계(초대 API는 있는데 프론트 라우트가 없어 링크가 데드링크). **단위 테스트는 경계 안쪽을 지키고, 사람 검수는 경계를 넘어가 본다**는 걸 배웠습니다."

---

## 1. 프로젝트 전반

### Q1-1. 이 프로젝트를 소개해주세요.

→ **[0번 오프닝 스크립트](#0-오프닝-스크립트)** 참조.

**팁**: 면접관 성향에 따라 순서를 바꾼다.

- 백엔드/아키텍처 중심 → 멀티테넌시 3계층을 먼저
- 보안 중심 → RBAC + 감사로그를 먼저
- 인프라 중심 → 배포 블로커 5건과 fail-fast를 먼저

---

### Q1-2. 왜 이 주제를 골랐나요?

**답변**

> "**도메인이 기술적 요구를 자연스럽게 만들어내는 주제**를 고르고 싶었습니다.
>
> QA 관리 SaaS는 억지로 기능을 붙이지 않아도 다음이 전부 필요합니다. 여러 조직이 같은 테이블을 공유하니 **멀티테넌시 격리**가 필수고, 같은 사람이 조직마다 다른 역할을 가지니 **토큰에 조직을 담을 수 없는 RBAC**가 나오고, 여러 명이 한 실행을 나눠 도니 **WebSocket 동기화**가 필요하고, 외주가 섞인 환경이라 **감사로그**가 요구되고, 통과율 대시보드 때문에 **집계·N+1 회피**가 나옵니다.
>
> 반대로 말하면, 기능 하나하나가 '포트폴리오용으로 넣은 것'이 아니라 **문제에서 파생된 것**이라 설계 근거를 설명할 수 있습니다. 그게 제가 주제 선정에서 가장 중요하게 본 기준입니다.
>
> 포트폴리오 다양성 관점에서도, 제 이전 프로젝트들이 실시간은 얕게(단순 알림) 다뤘고 인증/보안 고도화도 하나뿐이어서 그 두 축을 동시에 깊게 파는 주제가 필요했습니다."

**꼬리질문: "실제로 쓰는 사람이 있나요?"**

> "실사용 서비스는 아닙니다. 다만 요구사항은 실제 QA 현장에서 반복되는 다섯 가지 문제(회차 추적 불가 / 동시 편집 충돌 / 이슈 수동 이관 / 외주 데이터 격리 / 변경 추적 불가)를 기준으로 잡았고, 그 다섯 개가 SPEC 문서의 첫 장입니다. 데모 계정으로 로그인하면 조직 2개, 실행 3건, 버그 3건이 채워진 상태로 바로 볼 수 있게 시드를 넣어뒀습니다."

---

### Q1-3. 개발 기간과 규모는요?

**답변**

> "L티어(2~3일)로 잡고 진행했습니다. 규모는 **API 엔드포인트 약 30개, 테이블 11개, 테스트 135개**(단위 22 + Testcontainers 통합 113)입니다.
>
> 코딩 전에 **설계 문서를 먼저 썼습니다** — SPEC(범위와 비범위), ARCHITECTURE(격리 전략과 기각한 대안), DATA-MODEL(스키마·인덱스·마이그레이션 전략), API(엔드포인트·에러 코드·WS 이벤트), 그리고 용어집까지요.
>
> 이게 시간을 **줄였다**고 생각합니다. 특히 **'비범위' 표를 먼저 못 박아둔 것**이 컸습니다. 파일 첨부, 이메일 발송, 소셜 로그인, 낙관적 잠금, Jira 연동 같은 게 계속 떠오르는데, 그때마다 '이건 이 프로젝트가 증명하려는 것(멀티테넌시·RBAC·실시간·감사)에 기여하는가?'를 표로 판단했습니다. 기여가 없으면 잘랐고, 그 이유를 README에 남겼습니다."

---

### Q1-4. 이 프로젝트에서 가장 어려웠던 부분은?

**답변**

> "**'실수해도 안전한 구조를 만드는 것'** 이 가장 어려웠습니다.
>
> 조직 격리를 하는 방법 자체는 어렵지 않습니다. 모든 쿼리에 `where: { organizationId }`를 붙이면 됩니다. 문제는 **쿼리 100개 중 1개를 빠뜨리면 격리가 없는 것과 같다**는 겁니다. 그리고 그 1개는 코드 리뷰로 잡히지 않습니다 — 새 기능을 추가하는 다음 사람이 빠뜨릴 수도 있으니까요.
>
> 그래서 '조심하기'가 아니라 **'조심할 필요가 없게 만들기'** 로 방향을 잡았습니다. Prisma Client Extension이 자동 주입하고, 컨텍스트가 없으면 예외를 던지고, 그래도 뚫리면 DB 복합 FK가 거부합니다. 감사로그도 같은 사고방식으로 — 트랜잭션 밖에서는 **타입 시스템이 호출 자체를 막게** 했습니다.
>
> 이 방향으로 설계하면 코드가 처음엔 복잡해 보입니다. 브랜드 타입이나 확장 클라이언트가 왜 필요한지 설명이 필요하니까요. 그래서 **'왜 이렇게 했는지'를 코드 주석과 문서에 남기는 것**까지가 이 작업의 일부라고 봤습니다."

**꼬리질문: "그런 구조가 과설계 아닌가요?"**

> "규모만 보면 그렇게 볼 수 있습니다. 다만 판단 기준을 **'지금 코드 양'이 아니라 '실수의 비용'** 으로 잡았습니다.
>
> 조직 데이터가 다른 조직에 새는 사고는 되돌릴 수 없고, 이 도메인에서는 계약 위반입니다. 반면 확장 코드는 파일 하나 100줄이고, 서비스 코드에는 흔적이 없습니다. **비용이 비대칭이면 확률로 판단하면 안 된다**고 생각했습니다.
>
> 반대로 **과설계라고 판단해서 뺀 것들도 있습니다.** Postgres RLS는 가장 강력한 4번째 방어선이지만, Neon 풀러 뒤에서 세션 변수를 안전하게 쓰려면 모든 쿼리를 트랜잭션으로 감싸야 하고 정책 관리 부담이 붙습니다. 3계층으로 충분하다고 판단하고 향후 과제로 문서화했습니다. Redis adapter도, 낙관적 잠금도 같은 기준으로 뺐습니다."

---

## 2. 기술 선택 이유

### Q2-1. 왜 NestJS인가요? Express로는 안 되나요?

**답변**

> "가능은 합니다. 다만 이 프로젝트의 핵심이 **'요청이 도메인 코드에 닿기 전에 4단계 검사를 통과한다'** 인데, NestJS가 그 자리를 프레임워크 차원에서 제공합니다.
>
> 구체적으로 **가드(Guard) 개념**이 정확히 필요했습니다. 인증(누구인가) / 테넌트(어느 조직인가) / 역할(무엇을 할 수 있나) / 리소스(이 실행에 배정됐나)는 서로 다른 질문이라 **분리된 가드 4개**로 두는 게 맞습니다. Express 미들웨어로도 되지만, 라우트마다 순서를 손으로 배열하다 보면 누락이 생깁니다. Nest는 컨트롤러/핸들러 단위 데코레이터로 선언하고, `@RequireRole()` 같은 커스텀 데코레이터 + `Reflector` 조합으로 **'선언은 컨트롤러에, 판단은 가드에'** 를 깔끔하게 나눌 수 있습니다.
>
> DI도 실질적으로 도움이 됐습니다. `TENANT_PRISMA`라는 토큰 하나로 '조직 스코프가 적용된 클라이언트'를 주입하니, 도메인 서비스는 **격리가 어떻게 구현되는지 모릅니다.** 나중에 RLS로 바꿔도 서비스 코드는 안 바뀝니다.
>
> 그리고 통합테스트에서 `moduleRef.get(AuditService)`로 실제 프로바이더를 꺼내 실패를 주입할 수 있었던 것도 DI 덕분입니다."

**꼬리질문: "NestJS의 단점은 뭐라고 보시나요?"**

> "**러닝커브와 마법(magic)** 입니다. 데코레이터와 DI가 무엇을 하는지 모르면 '왜 이게 되는지'를 설명할 수 없고, 그 상태로 쓰면 디버깅이 어렵습니다. 실제로 이 프로젝트에서도 **가드 실행 순서가 곧 계약**이라 순서를 잘못 두면 `RunAssignmentGuard`가 아직 안 채워진 ALS를 읽게 됩니다. 그래서 가드 파일마다 '이 가드는 OrgContextGuard 다음에 실행돼야 한다'는 전제를 주석으로 남겼습니다.
>
> 또 하나는 **번들 크기와 부팅 시간**입니다. 컴포넌트 스캔과 프록시 생성이 있어서 Express보다 무겁습니다. Render 무료 티어 콜드스타트에는 불리한데, 이 프로젝트에선 감수했습니다."

---

### Q2-2. Prisma를 고른 이유는? TypeORM은요?

**답변**

> "세 가지 이유입니다.
>
> **첫째, 타입 안정성**입니다. Prisma는 스키마에서 클라이언트를 생성해서 `select`/`include` 조합에 따라 반환 타입이 정확히 좁혀집니다. 예를 들어 대시보드에서 `select`로 스칼라만 뽑으면 반환 타입에도 관계 필드가 없어서, 실수로 접근하면 컴파일 에러가 납니다.
>
> **둘째, 이 프로젝트의 핵심 기능이 Prisma의 확장 지점에 딱 맞았습니다.** Client Extension의 `query` 컴포넌트로 모든 쿼리를 가로채 `organizationId`를 주입할 수 있는데, TypeORM에는 이만큼 깔끔한 전역 훅이 없습니다(구독자나 커스텀 리포지토리를 써야 하는데 우회가 쉽습니다).
>
> **셋째, 마이그레이션 워크플로우**가 명확합니다. `migrate dev`로 개발, `migrate deploy`로 배포이고, 테스트도 배포와 **똑같은 `migrate deploy`** 를 씁니다.
>
> ⚠️ 다만 **TypeORM도 다른 프로젝트에서 써봤습니다.** 둘의 차이를 물으신다면, TypeORM은 엔티티 클래스가 도메인 객체 역할까지 하려는 Active Record/Data Mapper 성향이고 Prisma는 **철저히 쿼리 빌더에 가깝습니다.** 도메인 로직을 엔티티에 넣고 싶다면 TypeORM이, 데이터 접근 계층을 얇게 유지하고 싶다면 Prisma가 맞다고 봅니다."

**꼬리질문: "Prisma의 단점은?"**

> "실제로 이 프로젝트에서 두 개를 겪었습니다.
>
> **하나는 DSL의 표현력 한계**입니다. 복합 외래키 중 한쪽이 nullable이거나 한 모델이 두 개의 복합 관계를 필요로 하는 구성은 Prisma DSL로 표현이 안 됩니다. 그래서 마이그레이션 SQL로 직접 제약을 넣었는데, **그러면 Prisma가 그 제약을 'drift'로 오인해 자동 마이그레이션에서 되돌려버립니다.** 이후 마이그레이션은 전부 `--create-only`로 만들고 SQL을 검토하는 규칙을 세워야 했습니다.
>
> **다른 하나는 관계 로딩 방식**입니다. `include`로 has-many를 걸면 Prisma가 별도 배치 쿼리를 발행해서 쿼리 수가 늘어납니다. 대시보드에서 쿼리 예산 4개를 지키려고 `include`를 빼고 `select`로 스칼라만 뽑았습니다. **ORM이 만드는 쿼리를 모르면 성능을 통제할 수 없다**는 걸 체감했습니다."

---

### Q2-3. 검증을 왜 Zod로 했나요? class-validator가 NestJS 표준 아닌가요?

**답변**

> "Nest 문서 기본값은 class-validator가 맞습니다. Zod를 고른 이유는 세 가지입니다.
>
> **첫째, 스키마와 타입이 절대 어긋나지 않습니다.** class-validator는 `@IsString()` 데코레이터와 `title: string` 타입 선언을 **사람이 맞춰야** 합니다. 한쪽만 고치면 조용히 어긋납니다. Zod는 스키마가 진실의 원천이고 타입은 `z.infer`로 파생됩니다.
>
> **둘째, 프론트와 같은 라이브러리**를 씁니다. 프론트 폼 검증도 Zod라 규칙을 같은 사고방식으로 유지할 수 있습니다.
>
> **셋째, 변환이 내장**입니다. 쿼리스트링은 전부 문자열인데 `z.coerce.number().int().min(1).max(100).default(20)` 한 줄로 파싱·범위검증·기본값이 끝납니다.
>
> `nestjs-zod`의 `createZodDto`로 Zod 스키마를 Nest DTO 클래스로 만들면 **Swagger 문서화도 자동**으로 됩니다."

**꼬리질문: "Zod 때문에 문제가 생긴 적은?"**

> "있습니다. 감사로그 기간 필터에 `z.coerce.date()`를 썼더니 **Swagger 문서 생성이 실패**했습니다. `nestjs-zod`가 Zod 스키마를 JSON Schema로 변환해 OpenAPI를 만드는데, **JSON Schema에는 Date 타입이 없어서** 변환기가 표현할 방법이 없었습니다.
>
> `z.string().datetime()`으로 바꿔서 **ISO 문자열로 받고 서비스 계층에서 Date로 변환**하도록 고쳤습니다. 검증 강도는 그대로고(ISO 8601 형식 검사), 오히려 API 계약이 명확해졌습니다.
>
> 여기서 배운 건 **'API 경계의 타입은 전송 가능한 표현이어야 한다'** 입니다. `Date`는 언어 내부 표현이지 wire format이 아닙니다. `z.coerce.*`는 편리하지만 문서화·직렬화 경계에서 새는 추상화가 될 수 있습니다."

> ⚠️ **정직 포인트**: `package.json`에 `class-validator`와 `class-transformer`가 아직 남아 있는데 소스 어디에서도 import되지 않습니다. Nest 스캐폴드 기본값이 남은 건데, 검수에서 지적받고 제거 대상으로 문서화했습니다. 먼저 말하는 게 낫습니다.

---

### Q2-4. socket.io를 왜 썼나요? 순수 WebSocket이나 SSE는요?

**답변**

> "요구사항이 **양방향 + 룸 기반 브로드캐스트 + 자동 재연결**이었고, socket.io가 이 셋을 다 갖고 있습니다.
>
> **SSE(Server-Sent Events)를 먼저 검토했습니다.** 실제로 이 프로젝트는 서버→클라이언트 이벤트가 대부분이라 SSE로도 됩니다. 다만 두 가지가 걸렸습니다. ① **룸 개념이 없어서** 어느 연결에 무엇을 보낼지를 직접 관리해야 합니다. ② 클라이언트→서버 메시지(`run:join`, `run:leave` 같은 룸 조인 요청)를 보내려면 별도 REST가 필요해서 결국 채널이 두 개가 됩니다.
>
> **순수 WebSocket**은 재연결·룸·ack를 전부 직접 구현해야 하는데, 그건 socket.io를 다시 만드는 일입니다.
>
> ⚠️ 다만 **socket.io = WebSocket이 아니라는 점**은 정확히 말씀드리고 싶습니다. socket.io는 자체 프로토콜이고, 기본적으로 HTTP 롱폴링으로 시작해 WebSocket으로 업그레이드합니다. 이 프로젝트는 **`transports: ['websocket']`로 폴백을 껐습니다** — Render 프록시 환경에서 업그레이드가 불안정하다는 판단이었습니다. 대신 WebSocket이 막힌 환경에서는 연결이 아예 안 된다는 트레이드오프를 감수했습니다."

---

### Q2-5. 프론트 상태 관리를 Zustand + TanStack Query로 나눈 이유는?

**답변**

> "**상태의 소유권을 나눈 것**입니다.
>
> 서버에서 온 데이터(스위트·케이스·실행·버그)는 전부 **TanStack Query**가 소유합니다. 캐싱·재검증·로딩/에러 상태·무한스크롤이 다 필요한데 이걸 직접 만들면 그게 곧 TanStack Query를 다시 만드는 일입니다.
>
> **Zustand**는 서버가 모르는 것만 갖습니다 — 토큰, 현재 선택한 조직, 토스트 메시지. 특히 **훅 밖에서 읽어야 하는 값**이라 Zustand가 적합했습니다. axios 인터셉터와 socket.io의 `auth` 콜백은 React 컴포넌트가 아니라서 훅을 못 쓰는데, `useAuthStore.getState()`로 바로 읽을 수 있습니다.
>
> 그리고 이 프로젝트에서 특히 중요한 규칙이 하나 있습니다. **조직 스코프 쿼리 키는 반드시 헬퍼 하나로만 만듭니다.**
>
> ```ts
> export function orgScopedKey(orgId, ...rest) {
>   return ["orgs", orgId ?? "no-org", ...rest] as const;
> }
> ```
>
> 조직을 전환했는데 쿼리 키에 `orgId`가 없으면, 전환 직후 화면이 **이전 조직의 캐시를 그대로 보여줍니다.** 서버가 아무리 완벽히 격리해도 브라우저에서 다른 회사 데이터가 한 프레임 보이는 사고가 납니다. 이걸 '조심하기'가 아니라 **키 생성 통로를 하나로 좁혀서** 막았고, 이유를 파일 주석에 남겼습니다."

---

## 3. 멀티테넌시 — 이 프로젝트의 심장

### Q3-1. 멀티테넌시를 어떻게 구현했나요?

**답변**

> "**3계층**입니다. 각 계층이 **다른 종류의 실패**를 막습니다.
>
> **1계층 — `OrgContextGuard` (인가 실패를 막는다).** 모든 테넌트 리소스가 `/api/orgs/:orgId/...` 경로라 조직이 URL에 드러납니다. 가드가 `(userId, orgId)`로 Membership을 조회해서, 없으면 **404**를 던집니다. 403이 아닌 이유는 뒤에 설명드리겠습니다. 조회된 Role은 AsyncLocalStorage에 넣어 이후 가드와 Prisma가 씁니다.
>
> **2계층 — Prisma Client Extension (개발자 실수를 막는다).** `$extends`의 `query` 컴포넌트로 모든 모델의 모든 연산을 감싸서, 테넌트 모델이면 `organizationId`를 자동 주입합니다. 읽기는 `where`에, `create`는 `data`에 넣습니다. **핵심은 컨텍스트가 없을 때 통과가 아니라 예외를 던진다**는 겁니다. 가드를 안 붙인 새 엔드포인트가 조용히 전 조직 데이터를 반환하는 사고를 원천 차단합니다.
>
> **3계층 — DB 복합 FK (앱 우회를 막는다).** 부모 테이블에 `@@unique([id, organizationId])`를 걸고, 자식이 `(parentId, organizationId)` 복합 외래키로 참조합니다. 그래서 **다른 조직의 부모를 가리키는 행은 DB가 INSERT를 거부**합니다. 서비스를 우회한 직접 쿼리도 막힙니다.
>
> 검수에서 세 계층을 전부 코드로 확인했고, 통합테스트 T-1~T-7이 각 계층을 검증합니다."

**꼬리질문: "3계층이 전부 필요한가요? 하나면 안 되나요?"**

> "각 계층이 막는 실패의 종류가 다릅니다.
>
> - **1계층만 있으면**: 가드는 완벽한데 서비스에서 `where`를 빠뜨린 쿼리 하나가 전 조직을 반환합니다. 가드는 '이 조직 멤버냐'만 확인하지 '이 쿼리가 그 조직만 보는가'는 모릅니다.
> - **2계층만 있으면**: 자동 주입은 되는데 **비멤버가 임의 orgId로 접근**할 수 있습니다. 확장은 '컨텍스트의 조직'만 볼 뿐, 그 조직에 접근할 권리가 있는지는 판단하지 않습니다.
> - **3계층만 있으면**: FK는 '조직 A의 케이스를 조직 B의 스위트에 붙이는' 오염은 막지만, **조회 자체는 못 막습니다.** `SELECT * FROM test_cases`는 여전히 전부 나옵니다.
>
> 즉 **하나로 다 되지 않습니다.** 인가·자동필터·무결성은 서로 다른 문제고, 각각의 해법이 다릅니다."

---

### Q3-2. 왜 403이 아니라 404를 주나요?

**답변**

> "**존재 은닉(existence hiding)** 때문입니다.
>
> 403은 '여기 있는데 너는 못 본다'는 뜻이라 **리소스의 존재를 알려줍니다.** 공격자가 조직 UUID를 무작위로 넣어보면서 403이 오는 것과 404가 오는 것을 구분하면 **어떤 조직이 실재하는지 스캔**할 수 있습니다. 조직 이름·규모를 유추하는 정보 유출로 이어질 수 있고, 경쟁사 관계에서는 그 자체가 민감할 수 있습니다.
>
> 그래서 **조직 경계 밖은 전부 404**입니다. 목록도, 상세도, 수정도 마찬가지고, 다른 조직의 리소스 id를 내 조직 경로에 넣어도 404입니다.
>
> 여기에 디테일이 하나 더 있습니다. **비UUID 문자열이 와도 404로 수렴**시킵니다.
>
> ```ts
> const membership = await this.prisma.membership.findUnique({...}).catch(() => null);
> ```
>
> Prisma가 UUID 형식 오류로 던지는 예외를 잡아서 404로 만듭니다. 안 그러면 `'abc'`는 400, 실재하지 않는 UUID는 404가 되어 **에러 종류로 'UUID 형식이면 존재는 한다'는 정보가 샙니다.**
>
> ⚠️ **반대로 403을 쓰는 자리도 명확합니다.** 조직 **안에서** 역할이 부족한 경우는 403입니다. 그 사용자는 조직 멤버라 조직의 존재를 이미 알고 있고, **관리자에게 권한을 요청할 수 있어야** 하기 때문입니다. 404를 주면 '기능이 없다'고 오해합니다. 즉 정책이 **'경계 밖 = 404, 경계 안 권한 부족 = 403'** 으로 갈립니다."

---

### Q3-3. Prisma Client Extension이 정확히 어떻게 동작하나요?

**답변**

> "`$extends`의 `query` 컴포넌트는 **쿼리를 가로채서 인자를 바꿔치기하는 훅**입니다.
>
> ```ts
> prisma.$extends({
>   name: "tenant-scope",
>   query: {
>     $allModels: {
>       async $allOperations({ model, operation, args, query }) {
>         if (!TENANT_MODELS.has(model)) return query(args);
>         const ctx = getRequestContext();
>         if (!ctx?.organizationId)
>           throw new MissingTenantContextError(model, operation);
>         return query(applyTenantScope(operation, args, ctx.organizationId));
>       },
>     },
>   },
> });
> ```
>
> `query(newArgs)`가 실제 실행이고, 그 전에 `args`를 변형합니다.
>
> **연산별 처리**가 다릅니다. `create`는 `data.organizationId`를 컨텍스트 값으로 **덮어쓰고**, `createMany`는 모든 행에, `upsert`는 `where`와 `create` 양쪽에, 나머지는 `where`에 주입합니다.
>
> `create`에서 **덮어쓰기**인 게 중요합니다. 클라이언트가 요청 body에 `organizationId: '<남의 조직>'`을 끼워 넣어도 마지막에 컨텍스트 값으로 덮여 무시됩니다. 통합테스트 T-4가 이걸 검증합니다.
>
> 그리고 **화이트리스트 방식**입니다. 8개 테넌트 모델만 대상이고, 제외 사유가 코드에 적혀 있습니다 — `User`/`RefreshToken`은 전역 아이덴티티, `Organization`은 테넌트 앵커 자체(컬럼이 없습니다), `AuditLog`는 `organizationId`가 nullable이라 '항상 채운다' 규칙과 맞지 않아 `AuditService`가 직접 책임집니다."

**꼬리질문: "`findUnique`에 `where`를 덮어쓰면 타입 에러가 안 나나요?"**

> "Prisma 4.5 이후의 **'extended where unique input'** 덕분에 됩니다. unique 셀렉터에 임의 스칼라 필터를 **추가로** 얹는 게 허용돼서, `findUnique({where: {id}})`가 `{where: {id, organizationId}}`가 됩니다.
>
> 그래서 다른 조직 id로 조회하면 **'없음'과 완전히 동일하게** 취급됩니다 — 존재 은닉 정책과도 자연스럽게 일치합니다. 이 근거를 확장 파일 주석에 남겨뒀습니다."

**꼬리질문: "확장을 우회해야 하는 경우는 없나요?"**

> "있습니다. **정확히 4곳**이고, 각각에 '왜 여기서만 예외인가' 주석이 있습니다.
>
> 1. **`OrgContextGuard`의 멤버십 조회** — 지금 조직 컨텍스트를 *확정하는 중*이라 아직 ALS가 비어 있습니다. 확장 클라이언트로 이 조회를 하면 `TENANT_CONTEXT_MISSING`이 납니다. 일종의 닭과 달걀입니다.
> 2. **`AuthService`** — `User`/`RefreshToken`은 전역 모델입니다.
> 3. **`InvitationsService.accept()`** — 토큰만으로 조직을 알아내야 해서, 조직을 모르는 상태로 조회해야 합니다.
> 4. **`RunsGateway` / `assertReadable`** — 소켓 핸드셰이크는 HTTP 미들웨어 체인을 안 타서 ALS store 자체가 없습니다.
>
> **예외를 없앤 게 아니라 '셀 수 있게' 만든 것**이 요점입니다. 검수자가 4곳을 전수 확인하고 전부 정당하다고 판정할 수 있었던 것도 이 구조 덕분입니다."

---

### Q3-4. 왜 Postgres RLS를 안 썼나요? (압박)

**답변**

> "RLS가 **가장 강력한 방어선**이라는 데는 동의합니다. 실제로 README에 **향후 과제 1순위**로 적어뒀습니다. 안 쓴 이유는 몰라서가 아니라 비용 계산 결과입니다.
>
> **① 커넥션 풀러 문제가 가장 큽니다.** RLS는 `SET LOCAL app.current_org = ...` 같은 **세션 변수**에 의존합니다. 그런데 Neon은 PgBouncer류 풀러 뒤에 있고, 트랜잭션 풀링 모드에서는 **세션이 요청 간에 재사용**됩니다. 즉 앞선 요청이 세팅한 세션 변수가 남아 다른 테넌트의 요청에 적용될 수 있습니다. 이걸 안전하게 하려면 **모든 쿼리를 트랜잭션으로 감싸고** `SET LOCAL`을 매번 걸어야 합니다 — 단순 조회 하나까지요.
>
> **② Prisma와의 궁합.** Prisma는 커넥션을 직접 관리해서 '이 쿼리 직전에 SET LOCAL을 실행한다'를 보장하려면 확장이나 미들웨어로 또 감싸야 합니다. 결국 제가 만든 확장과 비슷한 것을 만들되, 거기에 RLS 정책 관리까지 얹는 구조가 됩니다.
>
> **③ 마이그레이션·시드 부담.** 테이블마다 정책을 만들고 유지해야 하고, 시드 스크립트나 마이그레이션처럼 **정당하게 전체를 봐야 하는 경로**를 위한 bypass 역할도 관리해야 합니다.
>
> 종합해서 **L티어 2~3일 범위에서 비용이 이득을 초과**한다고 판단했고, 대신 앱 3계층 + DB 복합 FK로 방어했습니다. 특히 3계층(복합 FK)은 **RLS 없이도 DB가 조직 경계를 강제**하는 부분이라, 앱이 완전히 뚫려도 데이터 오염은 막힙니다."

**꼬리질문: "그럼 RLS를 도입한다면 뭐가 바뀌나요?"**

> "**조회 유출까지 DB가 막게 됩니다.** 지금 복합 FK는 '다른 조직의 부모를 참조하는 행'은 막지만, `SELECT * FROM test_cases`처럼 필터 없는 조회는 못 막습니다. RLS가 있으면 그 쿼리조차 현재 세션의 조직 행만 반환합니다.
>
> 도입한다면 순서는 이렇게 잡겠습니다. ① 모든 요청을 트랜잭션으로 감싸는 실행기를 만들고(이미 `TenantTransactionService`가 있으니 그걸 확장) ② `SET LOCAL`을 그 안에서 걸고 ③ 테이블별 정책을 마이그레이션에 넣고 ④ **기존 3계층은 그대로 둡니다.** RLS는 4번째 계층이지 대체재가 아닙니다."

---

### Q3-5. `organizationId`를 모든 테이블에 중복 저장한 건 비정규화 아닌가요? (압박)

**답변**

> "맞습니다. **의도적 비정규화**이고, 이유가 두 개입니다.
>
> **① 확장이 모델별 분기 없이 동작합니다.** 모든 테넌트 테이블에 `organizationId`가 직접 있으면 `where.organizationId = ctx.organizationId` **한 줄**로 끝납니다. 만약 `TestCase`가 `suiteId`를 타고 조직을 유도해야 한다면, 확장이 모델마다 다른 조인 경로를 알아야 합니다 — 그 순간 확장은 유지 불가능해집니다.
>
> **② 모든 인덱스의 선두 컬럼을 `organization_id`로 만들 수 있습니다.** 테넌트 로컬리티라고 하는데, 인덱스 스캔 범위가 조직별로 자연 분할됩니다. 조직이 100개여도 한 조직의 쿼리는 자기 구간만 훑습니다.
>
> **그리고 비정규화의 위험은 복합 FK가 정확히 막습니다.** 비정규화가 위험한 이유는 '부모와 자식의 값이 어긋나는 것'인데, `(suiteId, organizationId) → (id, organizationId)` 복합 FK가 있으면 **어긋난 행은 DB가 INSERT를 거부**합니다. 즉 **비정규화와 복합 FK는 세트**입니다. 하나만 하면 위험하고, 둘을 같이 하면 안전하면서 빠릅니다."

---

### Q3-6. AsyncLocalStorage를 처음 써봤다고 했는데, 왜 그걸 골랐나요?

**답변**

> "확장이 '지금 이 쿼리는 어느 조직 것인가'를 알아야 하는데, 방법이 세 가지였습니다.
>
> **① 모든 서비스 메서드에 `orgId`를 인자로 전달** — 시그니처가 전부 오염되고, 무엇보다 **한 곳이라도 빠뜨리면 격리가 뚫립니다.** 제가 피하려던 '사람의 기억에 의존하는 안전'으로 되돌아갑니다.
>
> **② Nest의 REQUEST 스코프 프로바이더** — 요청마다 DI 트리를 새로 만들어 성능 비용이 있고, Prisma 확장은 DI 밖(팩토리)에서 만들어져 접근이 어색합니다.
>
> **③ AsyncLocalStorage** — Node 내장이라 **새 의존성이 0**이고, 확장 함수에서 `getRequestContext()` 한 줄로 접근합니다. 이걸 골랐습니다.
>
> 동작 원리는 **비동기 실행 컨텍스트에 값을 매다는 것**입니다. `storage.run(store, fn)`으로 시작하면, `fn`에서 파생된 모든 `await` 체인이 같은 store를 봅니다. Promise 체인을 넘어가도 유지됩니다.
>
> 이 프로젝트에선 store를 **참조로 공유**해서, 미들웨어가 `{ip, userAgent}`만 담아 만들고, JwtAuthGuard가 `userId`를, OrgContextGuard가 `organizationId`/`role`을 **같은 객체에 얹습니다.** 그래서 이후 어디서 읽어도 최신 값이 보입니다."

**꼬리질문: "ALS의 위험은 없나요?"**

> "두 개 있습니다.
>
> **① '흐름 밖'에서는 없습니다.** 소켓 핸드셰이크는 HTTP 미들웨어를 안 타서 store가 아예 없고, 시드 스크립트나 단위테스트도 마찬가지입니다. 그래서 그 경로들은 원본 Prisma를 쓰거나, 시드처럼 `runWithRequestContext({...}, () => ...)`로 컨텍스트를 직접 열어줍니다. **이 경계를 정확히 아는 게 ALS 사용의 핵심**이라고 봅니다.
>
> **② 암묵적 의존성이 생깁니다.** 함수 시그니처만 봐서는 `orgId`에 의존한다는 걸 알 수 없습니다. 전역 변수와 비슷한 냄새가 나죠. 그래서 이 프로젝트는 ALS를 **인프라 계층(확장·가드·감사로그)에만** 쓰고, 도메인 로직이 컨텍스트를 여기저기서 읽지 않도록 제한했습니다. 실제로 도메인 서비스에서 `getRequestContext()`를 부르는 곳은 organizationId가 정말 필요한 소수(이벤트 emit용 룸 이름 등)뿐입니다.
>
> ⚠️ 성능에 대해서는 **'AsyncLocalStorage는 공짜가 아니다'** 는 것도 알고 있습니다. 비동기 컨텍스트 추적 오버헤드가 있는데, 최근 Node 버전에서 상당히 개선됐고 이 프로젝트 규모에선 DB 왕복 대비 무시할 수준이라 판단했습니다."

---

## 4. RBAC / 인가

### Q4-1. 권한 체계를 설명해주세요.

**답변**

> "역할이 4개고 **등급이 있습니다**: `ADMIN(3) > QA_LEAD(2) > TESTER(1) > VIEWER(0)`.
>
> 중요한 건 **역할이 사용자가 아니라 `Membership`(사용자 × 조직)에 붙는다**는 겁니다. 같은 사람이 조직 A에선 ADMIN, 조직 B에선 VIEWER일 수 있습니다. 데모 계정이 정확히 그렇게 시드돼 있어서, 조직 전환기에서 역할 배지가 바뀌고 쓰기 버튼이 사라지는 걸 바로 볼 수 있습니다.
>
> 구현은 `ROLE_RANK` 상수 하나로 등급을 표현하고, `@RequireRole(Role.TESTER)`는 'TESTER 이상'을 뜻합니다. `RolesGuard`가 `Reflector`로 데코레이터 메타데이터를 읽어 판단합니다.
>
> **인가가 실제로는 3층입니다.**
>
> 1. **역할 등급** — `RolesGuard`
> 2. **리소스 수준** — `RunAssignmentGuard`('TESTER는 배정된 실행만')
> 3. **테넌트 경계** — Prisma Extension
>
> 각각이 다른 질문에 답합니다. 역할만으로는 '이 사람이 이 실행에 배정됐나'를 표현할 수 없어서 2번이 따로 필요합니다."

---

### Q4-2. ★왜 Role을 JWT에 넣지 않았나요?

**답변** — 이 프로젝트의 대표 설계 질문이다.

> "**JWT는 서버가 무효화할 수 없기 때문**입니다.
>
> 토큰에 `{ sub, orgId, role }`을 박으면 요청마다 DB 조회가 없어 빠릅니다. 흔한 구현이기도 합니다. 그런데 문제가 있습니다.
>
> ADMIN이 어떤 외주 인력을 **TESTER에서 VIEWER로 강등하거나 조직에서 추방해도, 그 사람의 토큰이 만료될 때까지(15분) 이전 권한으로 계속 쓰기가 가능**합니다. 외주·계약직이 섞이는 QA 도메인에서 이건 '불편'이 아니라 **실제 보안 결함**입니다. 계약 해지된 사람이 15분간 데이터를 수정할 수 있다는 뜻이니까요.
>
> 그래서 나눴습니다.
>
> - **토큰**: '너는 **누구**인가' — `{ sub, email, jti }`만
> - **요청 시점 Membership 조회**: '이 조직에서 **무엇을 할 수 있나**'
>
> 조직 선택은 **경로 파라미터**(`/api/orgs/:orgId/...`)로 표현하니 서버는 완전히 무상태이고, 조직이 Swagger·로그·테스트에 눈에 보입니다.
>
> **대가는 요청당 쿼리 1회**입니다. 다만 `@@unique([userId, organizationId])` 인덱스를 정확히 타는 PK 룩업 수준이고, 요청당 이미 여러 쿼리가 나가는 상황에서 +1은 비율상 미미합니다. 필요해지면 짧은 TTL 캐시로 최적화할 여지도 남겨뒀습니다.
>
> 그리고 이걸 **테스트로 잠갔습니다.** T-12가 '역할을 TESTER→VIEWER로 강등한 **직후 기존 access token**으로 쓰기 시도 → 403'을 검증합니다. 토큰을 재발급받지 않았는데 권한이 바뀌었다는 게 증거입니다."

**꼬리질문: "그 조회 1회가 부담되는 규모가 되면요?"**

> "**짧은 TTL 캐시**를 넣겠습니다. 예를 들어 `(userId, orgId) → role`을 5초 TTL로 캐싱하면, 강등 반영 지연이 **최대 5초**로 명시적으로 관리됩니다. 토큰 만료(15분)와 결정적으로 다른 점은 **상한을 제가 정한다**는 것이고, 강등 시 해당 키를 즉시 무효화하면 지연을 0으로 만들 수도 있습니다.
>
> 다만 그건 **캐시 무효화 문제**를 새로 들이는 것이라, 실제로 병목이라는 측정이 나오기 전에는 안 넣는 게 맞다고 봅니다. 지금은 요청당 1 인덱스 룩업이고, Neon 리전을 백엔드와 맞춰 왕복 지연도 줄여뒀습니다."

**꼬리질문: "그럼 토큰 무효화가 필요한 상황(로그아웃, 계정 정지)은요?"**

> "정직하게 말씀드리면 **access token 자체의 무효화는 이 프로젝트에 없습니다.** 15분 안에는 유효합니다.
>
> 대신 **refresh 토큰은 DB에 상태가 있어서 즉시 폐기 가능**합니다. 로그아웃하면 refresh가 폐기되므로 15분 뒤에는 갱신이 안 되고 세션이 끝납니다. 계정 정지도 마찬가지입니다.
>
> 즉 **'권한 변경은 즉시 반영, 세션 종료는 최대 15분 지연'** 이라는 절충입니다. 세션 종료도 즉시여야 한다면 access token에 `jti`를 이미 넣어뒀으니(현재는 발급 추적용) **폐기 목록(denylist)** 을 Redis에 두고 가드에서 확인하는 방식이 표준이고, 그건 Redis를 들이는 시점에 함께 하겠습니다."

---

### Q4-3. `RunAssignmentGuard`는 왜 따로 있나요?

**답변**

> "**'TESTER는 자신이 배정된 실행만 기록 가능'** 이라는 규칙이 역할 검사로 표현이 안 되기 때문입니다. 같은 TESTER라도 실행마다 답이 다릅니다 — 이건 **리소스 수준 인가**입니다.
>
> ```ts
> if (ctx?.role && ROLE_RANK[ctx.role] >= ROLE_RANK[Role.QA_LEAD]) return true; // 리드+는 배정 무관
> const assignment = await this.prisma.testRunAssignee.findUnique({
>   where: { testRunId_userId: { testRunId: runId, userId } },
> });
> if (!assignment) throw new DomainException(403, "RUN_NOT_ASSIGNED", "...");
> ```
>
> 세 가지 디테일이 있습니다.
>
> **① 순위표를 재사용합니다.** `RolesGuard`가 쓰는 `ROLE_RANK`를 그대로 씁니다. 권한 서열이 코드에 두 번 나오면 한쪽만 고치는 사고가 납니다.
>
> **② `TestRunAssignee`의 PK가 `(testRunId, userId)`** 라서 이 조회가 곧 PK 룩업입니다. 가드는 쓰기 요청마다 도는 핫패스라, **인덱스 설계로 가드를 지원**한 예입니다.
>
> **③ 이 가드는 확장 클라이언트(`TENANT_PRISMA`)를 씁니다.** `OrgContextGuard`가 **먼저** 실행돼 ALS를 채워뒀기 때문에 안전합니다. 가드 실행 순서가 곧 계약이라, 그 전제를 파일 주석에 남겼습니다."

**꼬리질문: "가드에서 DB를 조회하는 게 맞나요? 서비스에서 하면 안 되나요?"**

> "**인가는 가드에, 비즈니스 규칙은 서비스에** 두는 게 제 기준입니다.
>
> '이 요청을 진행시킬지 말지'는 인가라 가드가 맞습니다. 서비스에 두면 ① 컨트롤러 핸들러가 실행된 뒤에 거부하게 되고 ② 같은 규칙이 필요한 다른 엔드포인트에서 재사용이 안 되고 ③ **Swagger 문서에 요구 권한이 안 드러납니다.**
>
> 다만 트레이드오프는 있습니다. 가드에서 조회한 `TestRunAssignee`를 서비스가 다시 필요로 하면 중복 조회가 됩니다. 이 프로젝트에선 서비스가 그 값을 안 쓰므로 문제가 없지만, 필요해지면 가드가 조회 결과를 request 객체에 실어 넘기는 방식이 표준입니다."

---

### Q4-4. 프론트에서 버튼을 숨기면 되지 않나요? (압박)

**답변**

> "**프론트 게이팅은 UX이지 보안이 아닙니다.** 둘 다 합니다.
>
> 프론트에서 역할에 따라 버튼을 숨기는 건 '누를 수 있는데 403이 뜨는' 나쁜 경험을 막기 위한 것이고, **서버는 프론트를 전혀 신뢰하지 않습니다.** 브라우저 콘솔에서 fetch를 직접 날리면 프론트 코드는 아무 의미가 없으니까요.
>
> 그래서 이 프로젝트는 **역할 매트릭스 테스트(T-8)** 로 잠갔습니다 — VIEWER/TESTER/QA_LEAD/ADMIN × 주요 쓰기 엔드포인트 조합을 실제 HTTP 요청으로 돌려서 표대로 200/403이 나오는지 검증합니다. **'프론트에서 안 보이니까 안전하다'가 아니라 '서버가 거부하는 걸 테스트가 증명한다'** 가 근거입니다.
>
> 그리고 프론트에는 403이 오면 전역 토스트를 띄우는 처리를 axios 인터셉터에 넣어뒀습니다 — 게이팅이 완벽하지 않아도 사용자가 무슨 일이 났는지 알 수 있게요."

---

## 5. 인증 (JWT · refresh 회전 · 재사용 탐지)

### Q5-1. 인증 흐름을 설명해주세요.

**답변**

> "**access / refresh 두 토큰** 구조입니다.
>
> | 토큰    | 수명 | 형태                                        | 저장                             |
> | ------- | ---- | ------------------------------------------- | -------------------------------- |
> | access  | 15분 | **JWT(HS256)**, payload `{sub, email, jti}` | 클라이언트 메모리 + localStorage |
> | refresh | 7일  | **불투명 토큰** — `randomBytes(32)`         | 서버 DB에 **SHA-256 해시만**     |
>
> 로그인하면 둘 다 받고, 이후 요청은 `Authorization: Bearer <access>`로 갑니다. access가 만료되면 클라이언트가 `/auth/refresh`로 새 쌍을 받습니다.
>
> **refresh를 JWT로 만들지 않은 게 핵심 결정**입니다. JWT는 자기 검증형이라 **서버가 무효화할 수 없습니다.** refresh는 반드시 폐기 가능해야 하므로 **DB에 상태를 두는 불투명 토큰**이 맞습니다.
>
> 그리고 **원문이 아니라 SHA-256 해시만 저장**합니다. DB가 유출돼도 토큰을 재구성할 수 없어야 하니까요. 비밀번호를 bcrypt로 저장하는 것과 같은 논리입니다."

**꼬리질문: "refresh는 왜 bcrypt가 아니라 SHA-256인가요?"**

> "**엔트로피가 다르기 때문**입니다.
>
> 비밀번호는 사람이 만들어서 엔트로피가 낮습니다(사전 공격·무차별 대입이 현실적). 그래서 **의도적으로 느린** 해시(bcrypt/argon2)로 공격 비용을 올립니다.
>
> refresh 토큰은 `randomBytes(32)` — **256비트 암호학적 랜덤**입니다. 무차별 대입이 계산상 불가능하니 느린 해시로 얻을 이득이 없고, 오히려 **매 갱신 요청마다 bcrypt를 도는 비용**만 생깁니다. 실제로 이 프로젝트는 Render 무료 CPU라 그 비용이 유의미합니다.
>
> ⚠️ 다만 **'해시를 쓰는 이유'는 같습니다** — DB 유출 시 원문 복원 방지. 목적은 같고 위협 모델이 달라 알고리즘 선택이 달라진 겁니다."

**꼬리질문: "bcrypt cost를 10으로 한 이유는?"**

> "**Render 무료 티어 CPU 기준의 트레이드오프**입니다. cost 12가 일반적 권장이지만, 무료 인스턴스에서 12는 콜드스타트 직후 로그인 응답이 눈에 띄게 느려집니다. 데모 첫인상에 직접 영향이 있어서 10으로 낮추고, **그 이유를 상수 옆 주석에 남겼습니다.**
>
> ⚠️ 실서비스라면 다르게 판단합니다. cost는 **하드웨어 성능에 맞춰 주기적으로 올리는 값**이고(무어의 법칙 대응), 로그인 응답 100ms 정도는 감수하는 게 맞습니다. 지금은 '무료 티어 데모'라는 제약이 명확해서 낮춘 것이고, 그 근거를 말할 수 있으면 감점 요인이 아니라고 봅니다."

---

### Q5-2. refresh 회전과 재사용 탐지가 뭔가요?

**답변**

> "**회전(rotation)**: refresh를 쓸 때마다 기존 것을 폐기하고 새로 발급합니다. 같은 `familyId`(계열 id)는 유지합니다.
>
> **재사용 탐지(reuse detection)**: 이미 폐기된 refresh가 다시 들어오면? 정상 사용자는 그럴 일이 없습니다 — 회전 후엔 새 토큰만 갖고 있으니까요. 그러므로 **누군가 옛 토큰을 복사해 갔다**는 신호입니다.
>
> 그때 **그 계열(familyId) 전체를 폐기**하고 `AUTH_REFRESH_REUSE_DETECTED` 감사로그를 남긴 뒤 401을 줍니다. 결과적으로 공격자와 정상 사용자 **둘 다** 로그아웃되는데, 이게 **의도**입니다. 탈취가 의심되는 순간 계열을 끊고 재로그인을 요구하는 게 안전합니다.
>
> 이 동작을 통합테스트 T-24가 검증합니다 — 회전 후 구 토큰 재사용 → 401 + 같은 familyId 전체 폐기 + 감사로그."

---

### Q5-3. ★재사용 탐지에서 실제로 겪은 버그가 있다고요?

**답변** — 강력한 스토리다. STAR로.

> "**상황(S)**: 재사용 탐지를 구현했고, 테스트를 짜면서 '계열 전체가 정말 폐기됐는지' DB를 직접 확인했습니다.
>
> **문제(T)**: **401은 정상적으로 나가는데, DB의 refresh 토큰들이 폐기되어 있지 않았습니다.** 보안 기능이 동작하는 척만 하고 있었던 겁니다. 공격자가 같은 토큰으로 계속 시도할 수 있고, 감사로그도 안 남습니다.
>
> **원인과 조치(A)**: 처음 구현이 이랬습니다.
>
> ```ts
> await prisma.$transaction(async (tx) => {
>   await tx.refreshToken.updateMany({
>     where: { familyId },
>     data: { revokedAt: new Date() },
>   });
>   throw new DomainException(401, "AUTH_REFRESH_REUSE", "..."); // ← 이 throw가 위 UPDATE까지 롤백시킨다
> });
> ```
>
> 인터랙티브 트랜잭션의 콜백에서 예외가 던져지면 **트랜잭션 전체가 ROLLBACK**됩니다. 예외 전에 실행한 UPDATE도 함께 사라집니다. 당연하다면 당연한데, '탐지했으니 폐기하고 던지자'는 흐름이 자연스러워서 놓쳤습니다.
>
> 폐기를 **그 자체로 완결된 별개의 작은 트랜잭션**으로 분리하고, 커밋된 **뒤에** throw하도록 고쳤습니다. 그리고 재발 방지를 위해 코드에 이유를 남겼습니다 — _'이 폐기는 throw 이후에도 롤백되지 않고 반드시 커밋돼 있어야 한다(실제로 겪은 버그)'_.
>
> **결과(R)**: 흥미로운 건 **같은 파일 안에 반대 사례가 있다**는 겁니다. 정상 회전은 '기존 폐기'와 '신규 발급'을 **한 트랜잭션에 묶습니다** — 둘 중 하나만 반영되면 계열이 끊기거나 중복 유효 토큰이 남으니까요.
>
> 여기서 배운 일반화는 **'트랜잭션 경계는 기술적으로 묶을 수 있는 범위가 아니라, 함께 성공하거나 함께 실패해야 하는 범위로 정한다'** 입니다."

**꼬리질문: "그럼 감사로그도 별개 트랜잭션인가요?"**

> "**폐기와 감사로그는 같은 트랜잭션**입니다. '계열을 폐기했다'는 사실과 '그 사실을 기록했다'는 함께 성공하거나 함께 실패해야 하니까요. 밖으로 나간 건 **401 응답**뿐입니다.
>
> 이게 이 프로젝트의 일관된 원칙입니다 — **도메인 변경과 감사로그는 항상 한 트랜잭션**입니다. 결과 기록도 (RunCase update + 카운터 + AuditLog) 3쿼리가 한 트랜잭션이고, 롤백되면 감사로그도 안 남습니다(T-21)."

---

### Q5-4. ★프론트에서 동시에 401이 나면 어떻게 되나요?

**답변** — 서버 정책과 클라이언트 구현이 짝을 이룬 사례.

> "**서버가 재사용 탐지를 하니까, 클라이언트가 refresh를 두 번 부르면 강제 로그아웃됩니다.** 이게 실제 문제였습니다.
>
> 대시보드처럼 여러 쿼리가 동시에 나가는 화면에서 access가 만료되면, 요청 3개가 **동시에 401**을 받습니다. 각자 독립적으로 refresh를 부르면:
>
> 1. 첫 번째가 성공 → 그 refresh 토큰은 폐기됨
> 2. 두 번째·세 번째가 **같은(이미 폐기된) 토큰**으로 refresh 시도
> 3. 서버가 '재사용'으로 판단 → **계열 전체 폐기 → 강제 로그아웃**
>
> 즉 **정상 사용자가 로그아웃당합니다.** 서버는 정확히 설계대로 동작하는데 클라이언트가 그 정책을 모르는 상태였던 거죠.
>
> 해결은 **in-flight refresh 공유(큐잉)** 입니다.
>
> ```ts
> let inFlightRefresh: Promise<string | null> | null = null;
> ...
> inFlightRefresh ??= refreshAccessToken().finally(() => { inFlightRefresh = null; });
> const newToken = await inFlightRefresh;
> ```
>
> `??=` 한 줄로 '없으면 만들고, 있으면 그걸 기다린다'가 표현됩니다. 세 요청이 **하나의 refresh 결과를 공유**합니다.
>
> 디테일이 둘 더 있습니다.
>
> - **refresh 전용 axios 인스턴스**를 따로 둡니다. 같은 인스턴스를 쓰면 refresh 자체가 401을 맞았을 때 인터셉터가 다시 refresh를 불러 **무한 루프**에 빠집니다.
> - **`/auth/login`, `/auth/register`, `/auth/refresh`의 401은 재시도 대상에서 제외**합니다. 그건 '토큰 만료'가 아니라 '자격 증명이 틀렸다'는 뜻이라 refresh를 시도할 이유가 없습니다.
>
> 이 이야기의 핵심은 **서버의 보안 정책이 클라이언트 구현을 제약한다**는 겁니다. 한쪽만 알면 나머지 한쪽이 버그가 됩니다."

---

### Q5-5. 토큰을 localStorage에 저장하는 건 XSS에 취약하지 않나요? (압박)

**답변**

> "**맞습니다.** httpOnly 쿠키가 XSS 관점에서는 우월합니다. 그럼에도 헤더 방식을 택한 이유와, 그 대가를 어떻게 줄였는지 말씀드리겠습니다.
>
> **배포 토폴로지가 결정적이었습니다.** 프론트는 Vercel, 백엔드는 Render로 **도메인이 다릅니다.** 쿠키로 하려면 `SameSite=None; Secure` 크로스 사이트 쿠키가 필요하고, 그러면 **CSRF 방어 세트**(토큰 발급·검증·회전)가 새로 필요해집니다. 게다가 브라우저의 서드파티 쿠키 정책이 계속 조여지는 상황이라 장기적으로도 불안합니다.
>
> 즉 **'XSS 위험'과 'CSRF 위험 + 추가 복잡도'** 를 저울질한 결과입니다. 이 규모에서 CSRF 방어를 제대로(그리고 검증까지) 갖추는 비용이 더 컸습니다.
>
> **대가를 줄인 방법**:
>
> - access 수명을 **15분**으로 짧게 — 탈취돼도 창이 좁습니다
> - refresh **회전 + 재사용 탐지** — 탈취된 refresh로 지속 접근하면 계열이 끊깁니다
> - **입력은 전부 Zod로 검증**하고 React가 기본적으로 XSS를 이스케이프합니다(`dangerouslySetInnerHTML`을 쓰는 곳이 없습니다)
> - 트레이드오프를 **README에 명시**했습니다
>
> ⚠️ **정직하게 말하면**: XSS가 실제로 터지면 15분짜리 access와 refresh를 둘 다 훔칠 수 있어서 완전한 방어는 아닙니다. **같은 도메인에 배포할 수 있는 환경이라면 httpOnly 쿠키 + CSRF 토큰이 1순위**라고 생각하고, 그렇게 답하겠습니다."

---

### Q5-6. 로그인 실패 시 "이메일이 없다"와 "비밀번호가 틀렸다"를 구분하나요?

**답변**

> "**구분하지 않습니다.** 둘 다 `401 AUTH_INVALID_CREDENTIALS`에 같은 메시지입니다.
>
> ```ts
> const passwordMatches = user
>   ? await bcrypt.compare(dto.password, user.passwordHash)
>   : false;
> if (!user || !passwordMatches) {
>   /* 감사로그 후 단일 예외 */
> }
> ```
>
> 구분하면 **계정 열거(user enumeration)** 가 가능합니다. 이메일 목록을 넣어보면서 '이 이메일은 가입돼 있다'를 알아낼 수 있고, 그건 그 자체로 정보 유출이며 표적 피싱의 재료가 됩니다.
>
> 그리고 **실패도 감사로그로 남깁니다.** 조직이 아직 정해지지 않은 시점이라 `recordGlobal()`로 `organizationId: null`로 남기고, **계정이 존재하지 않는 경우에도 시도한 이메일을 `actorEmail`로 스냅샷**합니다. 나중에 '어떤 이메일로 반복 시도가 있었나'를 볼 수 있어야 하니까요.
>
> ⚠️ **알고 있는 한계**: 메시지는 통일했지만 **타이밍 사이드 채널**이 남아 있습니다. 계정이 없으면 bcrypt 비교를 건너뛰어 응답이 빠릅니다 — 응답 시간 차이로 계정 존재를 추론할 수 있습니다. 표준 대응은 **계정이 없을 때도 더미 해시로 bcrypt를 한 번 돌리는 것**이고, 개선 목록에 있습니다.
>
> 그리고 **로그인 브루트포스 방어가 없습니다.** 공개 데모라 `admin/admin`이 알려진 상태에서 무제한 시도가 가능합니다. 넣는다면 `@nestjs/throttler`를 로그인 라우트에만 한정 적용하는 게 맞다고 봅니다."

---

### Q5-7. 데모 계정(`admin`/`admin`)은 보안 구멍 아닌가요? (압박 — 반드시 대비)

**답변**

> "**우회한 것은 딱 하나, '이메일 형식 검증'뿐이고, 인증 절차 자체는 전혀 우회하지 않습니다.**
>
> ```ts
> const loginEmailSchema = z.string().min(1, '...')
>   .refine((v) => v === 'admin' || z.string().email().safeParse(v).success, {...});
> ```
>
> **우회 범위**: 문자열 `'admin'`이라는 **완전 일치 리터럴 하나**가 이메일 형식 검사를 통과합니다. 그게 전부입니다. `'not-an-email'` 같은 다른 비이메일 문자열은 여전히 400으로 거부됩니다.
>
> **적용 범위**: **로그인 스키마에만** 있습니다. 회원가입 스키마는 `z.string().email()`이라 `'admin'`으로 **가입 자체가 불가능**합니다. 일반 사용자는 항상 진짜 이메일로 가입합니다.
>
> **우회하지 않은 것**:
>
> - **비밀번호는 항상 bcrypt 비교를 통과해야 합니다.** `AuthService.login`에 admin 분기가 **0건**입니다. 틀린 비밀번호면 admin도 401이고, 그걸 테스트(T-26)가 잠급니다.
> - **인증 없이 토큰을 발급하는 엔드포인트가 없습니다.** 그런 걸 만들면 그게 백도어입니다.
>
> **위협 모델로 따지면**: 공격자가 이 예외로 얻는 건 '`admin`이라는 문자열을 이메일 필드에 넣을 수 있다'는 것뿐입니다. 다른 계정을 뚫을 수도, 권한을 올릴 수도 없습니다. 비밀번호가 공개된 데모 계정에 로그인하는 건 **의도된 기능**입니다.
>
> 덧붙이면 **시드도 정상 경로로 만듭니다.** Prisma로 행을 직접 꽂는 게 아니라 `AuthService.register()`와 `login()`을 **실제로 호출**합니다. 그래서 비밀번호는 진짜 bcrypt 해시가 되고 `AUTH_LOGIN_SUCCEEDED` 감사로그도 정상 경로로 남습니다. **시드 경로와 실사용 경로가 갈라지지 않습니다.**"

**꼬리질문: "그래도 프로덕션에 그런 예외가 있는 게 불편하지 않나요?"**

> "동의합니다. 실서비스라면 넣지 않습니다. **포트폴리오라는 맥락에서 '면접관이 회원가입 없이 완성된 화면을 봐야 한다'는 요구**가 있어서 넣은 것이고, 그 대신 **예외의 범위를 최소화하고 문서화**했습니다.
>
> 제가 이 패턴에서 중요하게 생각한 기준이 세 가지입니다. ① 예외는 **완전 일치 리터럴 1건**으로 좁힌다 ② **인증 절차(비밀번호 검증)는 절대 건드리지 않는다** ③ **우회 엔드포인트를 만들지 않는다.**
>
> 실제로 이전 프로젝트에서 **인증 없이 ADMIN 계정을 만드는 엔드포인트**를 발견해 제거한 적이 있습니다. 그게 진짜 백도어이고, 지금 패턴과는 성격이 완전히 다릅니다. 그 차이를 구분할 수 있다는 게 이 답변의 핵심입니다."

---

> **1부 끝.** 실시간·감사로그·DB/성능·트러블슈팅 STAR·배포·테스트·프론트·압박 질문·한계·역질문은 **[INTERVIEW-2.md](./INTERVIEW-2.md)** 로 이어진다.
