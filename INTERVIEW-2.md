# INTERVIEW — Runboard 면접 대비 (2부)

> [1부(INTERVIEW.md)](./INTERVIEW.md)에서 이어진다. 1부는 오프닝·프로젝트 전반·기술 선택·멀티테넌시·RBAC/인증을 다뤘다.
> 개념 설명은 [STUDY.md](./STUDY.md) · [STUDY-2.md](./STUDY-2.md).

---

## 🎯 컷시트 (면접 당일 5분 훑어보기)

> 오프닝 스크립트(0장)는 1부에 있다. 2부만 먼저 펴봤을 때를 위해 그대로 옮겨 둔다.

- **30초 소개**: 여러 조직이 하나의 DB를 공유하면서도 서로의 데이터를 절대 볼 수 없어야 하는 멀티테넌트 구조가 핵심이고, 여러 명이 같은 테스트 실행을 동시에 진행하면 WebSocket으로 실시간 동기화된다. 격리를 애플리케이션 한 곳에만 맡기지 않고 가드·Prisma Client Extension·DB 복합 FK 세 계층으로 구현한 게 가장 신경 쓴 부분이다. (1부 0장 오프닝 스크립트)
- **가장 어려웠던 문제**: "실수해도 안전한 구조를 만드는 것"이었다 — 쿼리 100개 중 1개를 빠뜨리면 격리가 없는 것과 같고, 그 1개는 코드 리뷰로 잡히지 않는다. "조심하기"가 아니라 "조심할 필요가 없게 만들기"로 방향을 잡았다. (1부 Q1-4)
- **대표 설계 질문**: Role을 JWT에 넣지 않은 이유 — 서버가 무효화할 수 없는 토큰에 권한을 박으면 강등·추방이 토큰 만료(15분)까지 반영되지 않는다. 요청 시점 Membership 조회로 대체했고, 대가는 요청당 인덱스 조회 1회다. (1부 Q4-2)
- **아쉬운 점**: 수평 확장 경험이 없다 — 인스턴스 1대 전제로 설계했고, 실제로 여러 인스턴스를 띄워 룸 브로드캐스트가 깨지는 걸 겪어본 적은 없다. (Q13-7)
- **데모 계정 압박 대비**: 우회한 것은 딱 하나, "이메일 형식 검증"뿐이고 인증 절차 자체는 전혀 우회하지 않는다. (1부 Q5-7)

---

## 목차 (2부)

6. [실시간 (WebSocket)](#6-실시간-websocket)
7. [감사로그](#7-감사로그)
8. [데이터베이스 / 성능](#8-데이터베이스--성능)
9. [트러블슈팅 STAR 5선](#9-트러블슈팅-star-5선)
10. [배포 / 인프라](#10-배포--인프라)
11. [테스트 / 품질 / 검수](#11-테스트--품질--검수)
12. [프론트엔드](#12-프론트엔드)
13. [★ 압박 질문 모음](#13--압박-질문-모음)
14. [내가 아는 한계 (먼저 말하기)](#14-내가-아는-한계-먼저-말하기)
15. [역질문 / 마무리](#15-역질문--마무리)
16. [절대 하면 안 되는 답변](#16-절대-하면-안-되는-답변)

---

## 6. 실시간 (WebSocket)

### Q6-1. 실시간 기능을 어떻게 설계했나요?

**답변**

> "**쓰기는 REST만, 소켓은 브로드캐스트 전용**입니다. 이게 첫 번째 결정이었습니다.
>
> 흐름은 이렇습니다. 테스터가 결과를 기록하면 REST로 `PATCH`가 가고, 가드 체인을 통과한 뒤 **한 트랜잭션에서 3쿼리**(RunCase 갱신 + TestRun 카운터 증감 + AuditLog 생성)를 실행합니다. **커밋이 끝난 뒤에만** 게이트웨이가 `run:case.recorded`와 `run:progress.updated`를 같은 실행 룸에 브로드캐스트합니다. 다른 브라우저는 그 이벤트로 TanStack Query 캐시를 패치합니다.
>
> 룸은 두 종류입니다 — `org:{orgId}`(조직 단위 알림: 버그 생성/수정, 실행 상태 변경)와 `run:{runId}`(실행 화면 동기화 + 프레즌스). `run:status.changed`는 **두 룸 모두**에 나갑니다. 실행 화면을 보는 사람도, 대시보드를 보는 사람도 갱신돼야 하니까요.
>
> 이벤트 페이로드는 **변경분 + 갱신된 카운터만** 보냅니다. 전체 목록을 다시 쏘지 않습니다. 비정규화 카운터가 있어서 집계 쿼리 없이 즉시 만들 수 있습니다."

---

### Q6-2. ★왜 소켓으로 쓰기를 안 받나요?

**답변**

> "**같은 규칙을 두 번 구현하지 않기 위해서**입니다.
>
> 쓰기를 소켓으로도 허용하면 다음이 전부 두 벌이 됩니다.
>
> | 규칙      | REST                | 소켓도 허용하면                      |
> | --------- | ------------------- | ------------------------------------ |
> | 입력 검증 | 전역 Zod 파이프     | 게이트웨이용 검증을 따로             |
> | 인가      | 가드 체인 4단계     | 소켓용 인가를 따로 — **누락 위험**   |
> | 트랜잭션  | 서비스가 `tx.run()` | 중복                                 |
> | 감사로그  | 트랜잭션 내부       | **여기서 빠지는 사고가 실제로 잦다** |
> | API 문서  | Swagger 자동        | 소켓은 자동 문서화가 없다            |
>
> 특히 **감사로그가 한쪽에서 빠지는 사고**가 현실적입니다. 이 프로젝트는 감사로그가 핵심 요구사항인데, '소켓으로 기록하면 로그가 안 남는' 상태가 되면 전체 신뢰도가 무너집니다.
>
> 그래서 **소켓을 읽기 전용 채널로 못 박은 것 자체가 설계 결정**이고, 그 근거를 ARCHITECTURE 문서에 남겼습니다.
>
> 대가는 **지연이 조금 늘어난다**는 것입니다 — 소켓으로 직접 보내면 HTTP 왕복을 아낄 수 있으니까요. 다만 로컬/스테이징 측정에서 p95 500ms 이내를 목표로 잡았고, 사람이 클릭하는 속도에서는 체감 차이가 없다고 판단했습니다."

---

### Q6-3. ★"emit은 커밋 이후에만"이 왜 중요한가요?

**답변**

> "**롤백된 상태를 다른 사용자에게 방송하지 않기 위해서**입니다.
>
> 트랜잭션 안에서 emit하면, 커밋되기 전에 이벤트가 나갑니다. 그 뒤 트랜잭션이 실패해 롤백되면 **존재하지 않는 상태가 다른 사람 화면에 남습니다.** 그 화면은 새로고침 전까지 거짓말을 하고, **이미 나간 메시지는 회수할 방법이 없습니다.**
>
> 구현은 오히려 단순합니다.
>
> ```ts
> const outcome = await this.tx.run(async (tx) => { /* update · 카운터 · 감사로그 */ });
> // ↑ 여기서 예외가 나면 아래 줄에 도달조차 못 한다
> this.events.emitCaseRecorded(runId, {...});
> ```
>
> **'커밋 후'를 조건문으로 확인하는 게 아니라, 코드 위치로 보장**합니다.
>
> 그리고 구조로 한 번 더 막았습니다. `run-events.service.ts`는 **트랜잭션을 아예 import하지 않습니다.** 그래서 '커밋 전에 emit'하는 코드가 그 파일에 섞여들 수가 없습니다. 파일 상단 주석에 그 의도를 적어뒀습니다."

**꼬리질문: "그걸 어떻게 증명했나요?"** — **가장 자신 있게 답할 부분**

> "**실패를 주입하는 통합테스트(T-16)** 를 만들었습니다.
>
> ```ts
> const spy = jest.spyOn(auditService, 'record')
>   .mockRejectedValueOnce(new Error('의도적으로 주입한 감사로그 실패'));
>
> const [res] = await Promise.all([
>   authed(qaLead).patch(`.../runs/${runId}/cases/${runCaseId}`).send({ result: 'FAIL' }),
>   assertNoEvent(watcherSocket, 'run:case.recorded', 1000),   // 1초간 이벤트가 안 오는 걸 확인
> ]);
> expect(res.status).toBe(500);
> expect((await prisma.testRunCase.findUnique(...))?.result).toBe('PENDING');   // 롤백됨
> expect((await prisma.testRun.findUnique(...))?.failedCount).toBe(0);           // 카운터도 롤백됨
> ```
>
> 포인트가 셋입니다.
>
> - **트랜잭션 도중 실패를 인위적으로 만들었습니다.** `moduleRef.get(AuditService)`로 실제 프로바이더를 꺼내 스파이를 걸 수 있는 게 Nest DI 덕분입니다.
> - **'일어나지 않음'을 검증했습니다.** `assertNoEvent`는 지정 시간 동안 이벤트가 오지 않으면 통과하는 헬퍼입니다. 부정 검증이라 타임아웃 기반이 될 수밖에 없습니다.
> - **목이 아니라 실제 Postgres 컨테이너 + 실제 소켓 연결**입니다. 트랜잭션 롤백은 DB 엔진의 동작이라 목으로는 증명이 안 됩니다.
>
> 세 가지가 동시에 검증됩니다 — DB 롤백, 카운터 롤백, 이벤트 미발행."

---

### Q6-4. 소켓 인증은 어떻게 하나요?

**답변**

> "**핸드셰이크에서만** 인증합니다.
>
> ```ts
> namespace.use((socket, next) => {
>   this.authenticate(socket)
>     .then(() => next())
>     .catch(() => next(new Error("인증에 실패했습니다.")));
> });
> ```
>
> 클라이언트가 `io(url, { auth: { token: accessToken } })`로 access token을 보내면, 게이트웨이 미들웨어가 **REST의 JwtStrategy와 같은 시크릿·같은 payload 모양**으로 검증하고 `socket.data`에 `{userId, email, name}`을 채웁니다. 실패하면 `next(err)`로 연결을 거부합니다.
>
> 이후 이벤트는 **이미 인증된 `socket.data`를 신뢰**합니다. 매 메시지마다 토큰을 재검증하지 않는 이유는, 연결 자체가 인증의 단위이기 때문입니다.
>
> **토큰 갱신 대응**이 재밌는 부분입니다. access가 15분마다 바뀌는데 소켓이 죽으면 안 되니까, 클라이언트에서 `auth`를 **고정 값이 아니라 함수**로 넘겼습니다.
>
> ```ts
> auth: (callback) => {
>   callback({ token: useAuthStore.getState().accessToken });
> };
> ```
>
> socket.io가 (재)연결을 시도할 때마다 이 함수를 다시 호출하므로, refresh로 토큰이 바뀐 뒤에도 **별도 배선 없이 항상 최신 토큰으로 재연결**됩니다."

---

### Q6-5. ★룸 조인 인가는 어떻게 하나요?

**답변**

> "**REST와 같은 인가 로직을 재사용합니다.** 이게 소켓에서 가장 흔한 사고를 막는 지점입니다.
>
> 사고 시나리오는 이렇습니다. `run:join { runId }`를 받아서 그냥 `socket.join('run:' + runId)`를 하면, **다른 조직 사람도 runId만 알면** 그 실행의 모든 결과 기록을 실시간으로 받아봅니다. REST는 404로 완벽히 막고 있는데 소켓으로 우회되는 거죠.
>
> ```ts
> const allowed = await this.runs.assertReadable(
>   body.orgId,
>   body.runId,
>   userId
> );
> if (!allowed) return { ok: false, code: "NOT_FOUND" };
> await socket.join(`run:${body.runId}`);
> ```
>
> `assertReadable`은 `RunsService`의 메서드입니다 — **REST가 쓰는 것과 같은 서비스, 같은 판단**입니다. 멤버십을 확인하고, 그 실행이 정말 그 조직 소속인지 확인합니다.
>
> **인가 로직이 두 벌이면 한쪽만 고치는 사고가 납니다.** '소켓이라고 검사를 건너뛰지 않는다'가 원칙이고, 그걸 서비스 메서드 재사용으로 구현했습니다. 통합테스트 T-7이 '조직 B 멤버가 조직 A 실행 룸에 join → 거부되고 이후 이벤트도 못 받는다'를 검증합니다.
>
> 한 가지 주의점은, **소켓 핸드셰이크는 HTTP 미들웨어를 안 타서 ALS 컨텍스트가 없다**는 겁니다. 그래서 `assertReadable`은 확장 클라이언트가 아니라 **원본 Prisma**를 쓰고, 그 이유를 주석에 남겼습니다."

---

### Q6-6. 이벤트가 유실되면 어떻게 하나요?

**답변**

> "**이벤트는 최적화이고, 정합성의 근거는 REST**라는 원칙으로 처리합니다.
>
> ```ts
> function join() {
>   socket.emit('run:join', { orgId, runId }, (ack) => { ... });
>   if (hasJoinedOnceRef.current) {                     // ★ "재연결"일 때만
>     queryClient.invalidateQueries({ queryKey: runDetailKey(orgId, runId) });
>     queryClient.invalidateQueries({ queryKey: runCasesKey(orgId, runId) });
>   }
>   hasJoinedOnceRef.current = true;
> }
> socket.on('connect', join);
> ```
>
> - **평소**엔 이벤트로 `setQueryData` 패치만 합니다. 500개짜리 실행에서 결과 하나 바뀔 때마다 전체를 다시 받으면 실시간의 의미가 없습니다.
> - **재연결 시**에는 룸을 다시 조인하고 **REST로 전체 재조회**합니다. 끊긴 동안 놓친 이벤트가 여기서 복구됩니다.
> - **최초 조인은 재조회를 하지 않습니다** — `useQuery`가 이미 최신 데이터를 가져온 직후라 낭비입니다. `hasJoinedOnceRef`로 구분합니다.
>
> 이게 실시간 시스템의 일반 패턴이라고 생각합니다. **이벤트를 신뢰의 유일한 근거로 삼으면 유실 시 복구 경로가 없습니다.**"

**꼬리질문: "동시에 같은 케이스를 두 명이 기록하면요?"**

> "**LWW(Last-Write-Wins)** 입니다. 마지막 기록이 이깁니다.
>
> 낙관적 잠금(버전 컬럼 + 충돌 시 409 + 해소 UI)을 검토했지만 **비범위로 잘랐습니다.** 이유는, 실사용 문제가 '충돌 해소'가 아니라 **'내가 모르는 사이에 덮였다'** 이기 때문입니다. 그래서 다른 방향으로 풀었습니다.
>
> - 이벤트 페이로드에 `recordedBy`와 `recordedAt`이 실립니다
> - 다른 사람이 기록하면 **`"OOO님이 방금 PASS 기록"` 토스트**가 뜹니다(내가 한 건 제외)
> - 화면의 해당 행이 즉시 갱신됩니다
>
> 즉 **덮어쓰기를 막는 대신, 덮어쓴 사람이 누군지 즉시 보이게** 했습니다. QA팀은 보통 케이스를 나눠 도는 구조라 충돌 자체가 드물고, 드문 충돌에 UX를 얹는 건 과설계라고 판단했습니다.
>
> ⚠️ 팀 규모가 커지거나 한 케이스를 여러 명이 검증하는 워크플로가 생기면 낙관적 잠금이 필요해질 수 있고, 그건 향후 과제에 적어뒀습니다."

---

### Q6-7. 서버를 여러 대로 늘리면 어떻게 되나요?

**답변**

> "**지금 구조는 인스턴스 1대 전제이고, 그걸 문서에 명시했습니다.**
>
> 깨지는 지점이 둘입니다.
>
> 1. **룸 브로드캐스트** — socket.io의 룸은 그 인스턴스에 연결된 소켓만 압니다. 인스턴스 A에서 emit하면 인스턴스 B에 붙은 사용자는 못 받습니다.
> 2. **프레즌스** — `RunPresenceService`가 인메모리라 인스턴스마다 다른 참여자 목록을 갖게 됩니다.
>
> 해법은 **`@socket.io/redis-adapter`** 입니다. Redis Pub/Sub으로 인스턴스 간에 룸 메시지를 전파하고, 프레즌스도 Redis에 두면 됩니다.
>
> **왜 지금 안 넣었나**: Render 무료 인스턴스 1대가 전제이고, Redis를 들이면 인프라 하나가 늘어납니다. 이 프로젝트에서 증명하려는 건 '실시간 동기화 설계'이지 '수평 확장'이 아니라고 판단했습니다. 대신 **확장 지점이 정확히 어디인지**(룸 브로드캐스트 + 프레즌스 저장소 두 곳)를 README에 적어뒀습니다.
>
> 프레즌스를 DB에 안 쓴 것도 의도입니다 — **휘발성 상태**라 서버가 죽으면 어차피 무의미하고, 접속/이탈마다 DB 쓰기가 발생하는 걸 피했습니다."

---

## 7. 감사로그

### Q7-1. 감사로그를 어떻게 남기나요?

**답변**

> "**도메인 서비스가 자기 트랜잭션 안에서** `AuditService.record(tx, ...)`를 호출합니다.
>
> ```ts
> await this.tx.run(async (tx) => {
>   const before = await tx.testCase.findUniqueOrThrow({ where: { id } });
>   const after = await tx.testCase.update({ where: { id }, data });
>   await this.audit.record(tx, {
>     action: "CASE_UPDATED",
>     targetType: "TEST_CASE",
>     targetId: id,
>     metadata: diffFields(before, after, ALLOWED_FIELDS),
>   });
>   return after;
> });
> ```
>
> **비즈니스 변경과 감사 기록이 원자적**입니다. 하나만 남으면 추적 신뢰도가 0이 됩니다 — '케이스는 바뀌었는데 누가 바꿨는지 모른다'가 되니까요. 통합테스트 T-21이 '트랜잭션 롤백 시 감사로그도 안 남는다'를 검증합니다.
>
> `actorId`, `organizationId`, `ip`, `userAgent`는 **ALS 컨텍스트에서 자동으로 채웁니다.** 호출부는 도메인 정보(action/target/metadata)만 신경 씁니다.
>
> 그리고 **`actorEmail`을 스냅샷으로 함께 저장**합니다. 멤버가 조직에서 제거돼도 로그가 '누구였는지'를 잃지 않도록요(T-22가 검증)."

---

### Q7-2. ★"타입으로 강제했다"는 게 무슨 뜻인가요?

**답변** — 이 프로젝트의 시그니처 설계.

> "**트랜잭션 밖에서는 `AuditService.record()`를 호출할 수 없게 타입을 설계**했습니다.
>
> 원칙만 정하면 사람이 어깁니다. '감사로그는 트랜잭션 안에서'라고 문서에 써도, 바쁘면 밖에서 부릅니다. 그래서 **브랜드 타입(branded type)** 을 썼습니다.
>
> ```ts
> declare const AUDIT_TX_BRAND: unique symbol;
> type AuditBrand = { readonly [AUDIT_TX_BRAND]: true };
>
> export type TenantAuditTransaction = Parameters<
>   Parameters<TenantPrismaClient["$transaction"]>[0]
> >[0] &
>   AuditBrand;
> ```
>
> ```ts
> async record(tx: TenantAuditTransaction, params: RecordScopedParams): Promise<void>
> ```
>
> TypeScript는 **구조적 타이핑**이라 '모양이 같으면 같은 타입'입니다. 그래서 평범한 `Prisma.TransactionClient`를 넘길 수 있어버립니다. 이걸 막으려고 **런타임에 존재하지 않는 고유 심볼 프로퍼티**를 타입에만 붙였습니다(`declare const`라 컴파일 후 사라집니다 — **런타임 오버헤드 0**).
>
> 이제 브랜드를 만드는 유일한 통로가 `TenantTransactionService.run()`(과 auth 전용 `runAuditableTransaction()`)뿐이라, **감사로그를 남기려면 반드시 트랜잭션을 열어야 합니다.**
>
> 효과는 **'감사로그 누락'을 코드 리뷰가 아니라 컴파일러가 막는 것**입니다."

**꼬리질문: "`as any`로 캐스팅하면 뚫리지 않나요?"**

> "**뚫립니다. 그 한계를 코드 주석에 명시했습니다.**
>
> > (TS 구조적 타이핑 한계상 100% 강제는 아니다 — 브랜드를 만드는 경로를 이 파일 하나로 좁혀 '직접 캐스팅하지 말 것'이라는 관례로 보강한다. 완전 강제가 필요해지면 eslint 커스텀 룰이 다음 단계.)
>
> 제가 이 설계에서 노린 건 '뚫을 수 없게 만드는 것'이 아니라 **'실수로는 뚫리지 않게 하고, 뚫으려면 의도적인 캐스팅이 필요하게 만드는 것'** 입니다. 의도적 캐스팅은 코드 리뷰에서 눈에 띕니다 — 반면 '트랜잭션 밖에서 record를 부르는 것'은 눈에 안 띕니다.
>
> 완전 강제가 필요하다면 ESLint 커스텀 룰로 `as unknown as TenantAuditTransaction` 패턴을 금지하는 게 다음 단계고, 그것도 문서화해뒀습니다."

---

### Q7-3. 조직이 정해지지 않은 이벤트(로그인)는 어떻게 하나요?

**답변**

> "**`recordGlobal()`** 이라는 별도 경로를 뒀습니다.
>
> 로그인은 인증이 **지금 일어나는 중**이라 ALS에 `userId`도 없고 조직도 없습니다. 그래서:
>
> |                | `record()`               | `recordGlobal()`                       |
> | -------------- | ------------------------ | -------------------------------------- |
> | 트랜잭션 타입  | `TenantAuditTransaction` | `RawAuditTransaction`(비확장)          |
> | organizationId | ALS에서 자동             | 항상 `null`                            |
> | actor          | ALS에서 자동             | **호출부가 명시적으로 전달**           |
> | 쓰는 곳        | 도메인 서비스 전부       | auth(로그인 성공/실패, refresh 재사용) |
>
> 이걸 위해 **마이그레이션으로 `AuditLog.organizationId`를 nullable로 바꿨습니다.** 설계 문서 원안은 NOT NULL이었는데, 구현하다 보니 '조직 미상 이벤트도 감사로그로 남기라'는 요구와 충돌한다는 걸 발견했습니다. **문서와 코드가 충돌하면 어느 쪽이 맞는지 판단해서 한쪽을 고쳐야 하고**, 여기선 요구사항이 맞아서 스키마를 바꿨습니다. 마이그레이션 파일 상단에 그 판단 근거를 남겼습니다.
>
> 그리고 **두 경로 모두 브랜드 타입**을 씁니다. 조직 스코프냐 전역이냐만 다르고, '트랜잭션 안에서만'이라는 원칙은 동일합니다."

---

### Q7-4. metadata에 민감정보가 새지 않나요?

**답변**

> "두 장치가 있습니다.
>
> **① 변경분만 담습니다.**
>
> ```ts
> export function diffFields(before, after, allowedFields) {
>   for (const field of allowedFields) {
>     if (JSON.stringify(before[field]) !== JSON.stringify(after[field]))
>       changed[field] = [before[field], after[field]];
>   }
> }
> ```
>
> 레코드 전체를 덤프하지 않고 실제로 바뀐 필드만 `{field: [before, after]}`로 남깁니다.
>
> `JSON.stringify` 비교인 이유가 있습니다. `steps` 같은 Json 필드는 **매번 새 배열 참조**라 `!==`이 항상 true입니다 — 내용이 같아도 '변경'으로 오탐합니다. 직렬화 비교면 원시값과 Json을 같은 방식으로 옳게 처리합니다.
>
> **② 화이트리스트입니다.** `allowedFields`에 없는 필드는 값이 달라도 **절대** 담기지 않습니다. 비밀번호·토큰 계열이 새는 경로가 원천 차단됩니다.
>
> ⚠️ **다만 알고 있는 구멍이 있습니다.** `cases.service.ts`와 `bugs.service.ts`가 `Object.keys(dto)`를 화이트리스트로 넘깁니다. 지금은 DTO 자체가 화이트리스트라 안전하지만, **나중에 DTO에 민감 필드가 늘면 그대로 새는 구조**입니다. 검수에서 지적받았고, 명시적 필드 배열로 좁히는 게 정답입니다."

---

### Q7-5. 감사로그를 수정하거나 지울 수 있나요?

**답변**

> "**없습니다. 조회 API만 있고 생성/수정/삭제 엔드포인트가 없습니다.** 조회도 ADMIN 전용입니다.
>
> **고칠 수 있으면 감사가 아니라고** 생각합니다. 사고가 났을 때 로그를 지울 수 있는 사람이 있으면 그 로그의 증거 능력이 0이 됩니다.
>
> 대가는 **테이블이 계속 자란다**는 것이고, 향후 과제로 `createdAt` 기준 파티셔닝을 적어뒀습니다. 지금 규모에선 조기 최적화라고 판단했습니다.
>
> 조회는 조직 스코프 + 필터(행위자/액션/대상/기간) + 커서 페이지네이션이고, **필터 4종에 1:1로 대응하는 복합 인덱스 4개**를 만들어뒀습니다."

---

## 8. 데이터베이스 / 성능

### Q8-1. N+1을 어떻게 막았나요?

**답변**

> "**세 지점에서 다른 방식**으로 막았습니다.
>
> **① 스위트 트리 — 1쿼리 + 메모리 조립.** 조직 전체 스위트를 평면으로 한 번에 조회하고 `Map`으로 부모-자식을 잇습니다. 재귀 쿼리도, 자식별 `findMany`도 안 씁니다. 최대 깊이가 3단계고 조직당 스위트 수가 적다는 전제가 명확해서 가능한 선택입니다.
>
> **② 대시보드 — 4쿼리 고정.** `groupBy` + `_sum`으로 **비정규화 카운터를 재사용**해서 '실행 상태별 개수'와 '결과 분포'를 같은 쿼리 하나로 얻습니다. `TestRunCase`를 다시 집계하지 않습니다.
>
> 여기서 의도적인 선택이 하나 있습니다. **최근 실행에 `include: { assignees }`를 안 씁니다.** Prisma가 has-many 관계를 별도 배치 쿼리로 로딩해서 쿼리가 2~3개 늘어나는데, '최근 실행 카드'에는 담당자가 필요 없으므로 `select`로 스칼라만 뽑아 1쿼리로 끝냅니다. **쿼리 예산을 지키려고 응답 필드를 줄인 것**이고, 그 이유를 주석에 남겼습니다.
>
> **③ 배치 조회 + Map.** 실행 케이스 목록에서 `recordedBy` 이름을 붙일 때, 케이스마다 `findUnique`를 부르는 대신 **id를 모아 `IN` 한 번**으로 조회하고 Map으로 매핑합니다.
>
> 그리고 **스냅샷 설계의 부수 효과**도 있습니다. `TestRunCase`가 제목·스텝을 복사해 갖고 있어서, 실행 상세에서 원본 케이스를 다시 읽을 일이 아예 없습니다."

---

### Q8-2. ★N+1을 막았다는 걸 어떻게 증명하나요?

**답변** — 강한 어필 포인트.

> "**실제 SQL 왕복 수를 세는 테스트 유틸**을 만들었습니다.
>
> ```ts
> export function attachQueryCounter(prisma: PrismaService): QueryCounter {
>   let count = 0;
>   prisma.$on("query", () => {
>     count += 1;
>   });
>   return {
>     reset: () => {
>       count = 0;
>     },
>     get count() {
>       return count;
>     },
>   };
> }
> ```
>
> 동작 원리가 두 가지입니다.
>
> - `PrismaService`가 `log: [{ emit: 'event', level: 'query' }]`로 쿼리 이벤트를 열어둡니다(리스너가 없으면 비용 없음).
> - **`$extends`는 같은 엔진 인스턴스를 감쌀 뿐**이라, 원본에 건 리스너가 `TENANT_PRISMA` 경유 쿼리까지 전부 잡습니다.
>
> 이게 있으면 'N+1을 막았다'가 **주장이 아니라 회귀 테스트로 고정**됩니다. 나중에 누가 `include`를 추가해서 쿼리가 늘면 테스트가 깨집니다.
>
> **'테스트로 잠그지 않은 성능 개선은 다음 커밋에서 사라진다'** 는 게 제 생각입니다."

---

### Q8-3. 비정규화 카운터는 정합성이 깨지지 않나요? (압박)

**답변**

> "깨질 수 있는 설계이고, 그래서 세 가지로 막았습니다.
>
> **① 갱신 경로가 하나입니다.** `applyCounterShift` 한 곳에서만 카운터를 건드립니다. 여러 곳에서 건드리면 반드시 어긋납니다.
>
> **② 같은 트랜잭션 안에서만 갱신합니다.** RunCase 결과 변경과 카운터 증감이 원자적이라, 하나만 반영되는 상황이 없습니다.
>
> **③ 원자적 증감을 씁니다.** `{ passedCount: { increment: 1 } }`는 SQL `SET "passedCount" = "passedCount" + 1`로 번역됩니다. 애플리케이션이 읽어서 더한 뒤 쓰는(read-modify-write) 게 아니라 **DB가 한 문장으로 처리**하므로 동시 요청에서도 어긋나지 않습니다.
>
> **그리고 실제로 여기서 버그가 났습니다.** 같은 결과를 두 번 기록하면 카운터가 이중 증가하는 문제였는데, 원인은 동시성이 아니라 **JS 객체 키 덮어쓰기**였습니다.
>
> ```ts
> if (prevField) data[prevField] = { decrement: 1 };
> if (nextField) data[nextField] = { increment: 1 }; // 같은 필드면 decrement가 사라진다
> ```
>
> **delta로 먼저 누적한 뒤 0이 아닌 필드만 반영**하도록 고쳤고, 자기 전이 회귀 테스트를 추가했습니다. 자세한 건 트러블슈팅 절에서 말씀드리겠습니다."

**꼬리질문: "정규화하면 이런 버그가 없었을 텐데요?"**

> "맞습니다. `COUNT(*) GROUP BY result`로 매번 세면 이 버그는 존재하지 않습니다. **그 트레이드오프를 알고 선택했습니다.**
>
> 정규화하면 결과 기록 한 번마다 실행의 전체 케이스를 훑어야 합니다. 케이스 500개짜리 실행에서 500번 기록하면 총 25만 행 스캔입니다. 게다가 **대시보드는 조직의 모든 실행에 대해 그걸 해야** 합니다.
>
> 비정규화의 대가로 얻은 건 **'결과 기록의 쿼리 수가 케이스 개수와 무관하게 3으로 고정'** 이고, 대시보드도 4쿼리 고정입니다.
>
> 그리고 **버그가 났다는 사실이 오히려 이 판단을 지지한다**고 봅니다. 버그의 원인은 비정규화 자체가 아니라 **증감 로직의 구현 실수**였고, 갱신 경로를 하나로 좁혀뒀기 때문에 **한 함수만 고쳐서 해결**됐습니다. 만약 카운터를 여러 곳에서 건드렸다면 훨씬 어려웠을 겁니다.
>
> ⚠️ 다만 **복구 경로가 없다**는 건 인정합니다. 카운터가 어긋났을 때 `TestRunCase`에서 재집계하는 `recount` 엔드포인트를 비범위로 뒀습니다. `test_run_cases(testRunId, result)` 인덱스는 그 재집계를 염두에 두고 만들어뒀습니다."

---

### Q8-4. 인덱스 설계를 설명해주세요.

**답변**

> "**실제 쿼리 → 인덱스**를 1:1로 매핑해서 문서에 표로 남겼습니다. 원칙은 두 개입니다.
>
> **① 복합 인덱스는 equality 조건을 앞에, 정렬/범위를 뒤에.**
>
> - `test_run_cases(testRunId, position)` — 실행 상세 화면(특정 실행 + position 정렬)
> - `bug_reports(organizationId, status, createdAt DESC)` — 조직 + 상태 필터 + 최신순
>
> **② 거의 모든 인덱스의 선두 컬럼이 `organization_id`.** 테넌트 로컬리티라고 하는데, 인덱스 스캔 범위가 조직별로 자연 분할됩니다.
>
> **핫패스를 특별히 지원한 것**이 둘 있습니다.
>
> - `memberships(userId, organizationId)` **unique** — 모든 요청이 도는 가드 조회라 PK 룩업 수준이어야 합니다.
> - `test_run_assignees`의 **PK 자체가 `(testRunId, userId)`** — 배정 확인 가드가 곧 PK 룩업입니다.
>
> 그리고 **감사로그는 필터 4종에 1:1로 대응하는 복합 인덱스 4개**를 만들었습니다: 기본 조회(`organizationId, createdAt DESC`), 대상별(`organizationId, targetType, targetId`), 행위자별, 액션별."

**꼬리질문: "인덱스를 그렇게 많이 만들면 쓰기가 느려지지 않나요?"**

> "맞습니다. 인덱스는 **읽기를 빠르게 하고 쓰기를 느리게** 합니다. 그래서 감사로그 4개는 의식적인 선택이었습니다 — 감사로그는 **쓰기가 많은 테이블**이라 인덱스 4개가 부담입니다.
>
> 정당화 근거는 ① 조회가 ADMIN 전용이지만 조회 시 필터 조합이 실제로 4종이고 ② 감사로그 쓰기는 이미 도메인 트랜잭션에 얹혀 있어 추가 왕복이 없으며 ③ 이 규모에서 인덱스 유지 비용은 무시할 수준이라는 겁니다.
>
> **규모가 커지면 판단이 바뀝니다.** 대량 적재 시점에는 파티셔닝을 먼저 하고, 인덱스도 실제 사용 빈도를 측정해서 줄이는 게 맞습니다."

---

### Q8-5. UUIDv7을 쓴 이유는?

**답변**

> "**인덱스 삽입 로컬리티** 때문입니다.
>
> UUIDv4는 완전 랜덤이라 B-tree 인덱스에 삽입될 때 **아무 데나 꽂힙니다.** 그러면 페이지 분할이 잦아지고 캐시 적중률이 떨어집니다. 특히 삽입이 많은 테이블(감사로그, 실행 케이스)에서 체감됩니다.
>
> UUIDv7은 **앞부분이 밀리초 타임스탬프**라 생성 순서대로 정렬됩니다. 새 행이 항상 인덱스의 오른쪽 끝에 붙어서 auto-increment와 비슷한 삽입 패턴을 갖습니다.
>
> 덤으로 **정렬 안정성**도 얻습니다. `createdAt`이 같은 행들 사이에서 id로 tie-break하면 대체로 생성 순서와 일치합니다.
>
> ⚠️ 다만 **이건 '완화'이지 '보장'이 아닙니다.** 같은 밀리초에 생성된 행들 사이의 순서는 보장되지 않습니다. 실제로 이 프로젝트의 커서 페이지네이션이 `orderBy: createdAt` + `cursor: {id}` 조합이라, 같은 타임스탬프가 여럿이면 경계에서 중복/누락이 가능합니다. 이건 검수에서 지적받은 항목이고, `orderBy: [{createdAt:'desc'}, {id:'desc'}]`로 타이브레이커를 추가하는 게 정답입니다.
>
> 그리고 **왜 그냥 auto-increment integer를 안 썼냐**면, ① id를 URL에 노출할 때 레코드 수가 추측되고 ② 분산 환경에서 생성이 어렵고 ③ 이 프로젝트는 조직 간 격리가 중요해서 **순차 id로 다른 조직 리소스를 추측하는 것**을 원천 차단하고 싶었기 때문입니다."

---

### Q8-6. 커서 페이지네이션을 쓴 이유는?

**답변**

> "**오프셋은 뒤로 갈수록 느려지고, 중복/누락이 생기기 때문**입니다.
>
> `OFFSET 1000`은 DB가 앞의 1000행을 **읽고 버려야** 합니다. 커서는 '이 id 다음부터'라 인덱스로 바로 점프합니다.
>
> 더 중요한 건 **일관성**입니다. 오프셋 방식에서 1페이지를 보는 동안 새 데이터가 앞에 삽입되면, 2페이지에서 이미 본 항목이 다시 나옵니다. 감사로그처럼 계속 쌓이는 데이터에서는 실제로 발생합니다.
>
> 구현은 `take + 1`로 한 건 더 받아 `hasMore`를 판정합니다.
>
> ```ts
> take: query.take + 1,
> ...(query.cursor ? { cursor: { id: query.cursor }, skip: 1 } : {}),
> ```
>
> '다음 페이지가 있나?'를 위해 `count(*)`를 따로 안 돌리기 위한 관용 기법입니다.
>
> ⚠️ 앞서 말씀드린 대로 **타이브레이커가 없는 게 현재 한계**입니다."

---

## 9. 트러블슈팅 STAR 5선

### T1. ★같은 결과 재기록 시 카운터 이중 증가 (배포 블로커)

> **S(상황)**: 배포 전 검수에서 카운터 로직이 의심스럽다는 지적이 나왔습니다.
>
> **T(과제)**: 코드를 읽고 끝내지 않고 **재현 테스트를 새로 작성**해서 실제 수치를 확보했습니다. 결과는 케이스 1개짜리 실행에 PASS를 두 번 기록했을 때 `totalCount: 1, passedCount: 2`. **진행률 200%, 통과율 200%** 가 대시보드에 뜨는 상태였습니다. UI가 동일 결과 재클릭을 막지 않아서 **면접관이 PASS를 두 번 누르면 바로 재현**됩니다.
>
> **A(조치)**: 원인은 동시성이 아니라 **JS 객체 키 덮어쓰기**였습니다.
>
> ```ts
> if (prevField) data[prevField] = { decrement: 1 };
> if (nextField) data[nextField] = { increment: 1 }; // prevField === nextField면 decrement가 덮인다
> ```
>
> PASS→PASS면 `data.passedCount`가 `{decrement:1}` → `{increment:1}`로 덮여서 감소가 사라집니다. 더 나빴던 건 **주석에 "같은 필드로 상쇄되는 경우도 자연히 처리된다"고 단언돼 있었다**는 것입니다 — 틀린 주석이 리뷰를 통과시키는 근거가 되고 있었습니다.
>
> 조기 반환으로 처리할 수도 있었지만 **delta 누적 방식**을 택했습니다.
>
> ```ts
> const delta = {};
> if (prevField) delta[prevField] = (delta[prevField] ?? 0) - 1;
> if (nextField) delta[nextField] = (delta[nextField] ?? 0) + 1;
> // 0이 아닌 필드만 increment/decrement로 변환
> ```
>
> 이유는 **특수 케이스를 분기로 처리하면 나중에 또 다른 특수 케이스가 생겼을 때 분기가 늘어나기 때문**입니다. delta는 일반화된 형태라 확장에도 맞습니다.
>
> 그리고 `computeCounters`에 **0~1 clamp**를 마지막 방어선으로 넣되, 주석에 "근본 수정은 카운터 계산 쪽에서 하고 이건 유사 회귀 대비"라고 명시했습니다. clamp를 근본 해결로 착각하지 않기 위해서입니다.
>
> **R(결과)**: 회귀 테스트를 추가했고, 전체 테스트가 그린입니다.
>
> **배운 것**: ① 상태 전이 로직은 **자기 전이(self-transition)** 를 반드시 테스트해야 합니다 — 기존 테스트는 FAIL→PASS처럼 **다른 필드 전이만** 검증해서 이 버그를 통과시켰습니다. ② **테스트가 그린인 것과 버그가 없는 것은 다릅니다.** ③ 주석이 동작을 단언하면 그 단언도 테스트로 잠가야 합니다.

---

### T2. ★Prisma가 손으로 넣은 복합 FK를 되돌린다 (drift)

> **S**: 감사로그의 `organizationId`를 nullable로 바꾸려고 `prisma migrate dev`를 실행했습니다.
>
> **T**: 생성된 마이그레이션에 **요청하지 않은 SQL**이 섞여 있었습니다. 3계층 방어선인 복합 FK 두 개(`test_suites_parent_same_org_fkey`, `bug_reports_runcase_same_org_fkey`)를 DROP하고 **단일 컬럼 FK로 되돌리는** 내용이었습니다.
>
> **A**: 원인은 Prisma의 **drift 검사 방식**이었습니다. `migrate dev`는 `schema.prisma`를 정답으로 보고 DB와의 차이를 계산합니다. 그런데 **복합 FK는 Prisma DSL이 표현하지 못해 스키마 파일에 없습니다.** 그래서 Prisma 입장에서는 "DB에 스키마엔 없는 제약이 있다" = drift → "정답에 맞춰 되돌리자"가 됩니다.
>
> 즉 **ORM이 모르는 DB 객체를 만들었다면, ORM의 자동 생성물이 그걸 지웁니다.**
>
> 이후 모든 마이그레이션은 **`--create-only`로 생성하고 SQL을 직접 검토·수정**하는 규칙으로 바꿨고, 그 이유를 마이그레이션 파일 최상단 주석에 남겼습니다.
>
> **R**: 3계층이 유지된 채로 스키마 변경을 마쳤고, 검수에서 세 계층이 전부 코드로 확인됐습니다.
>
> **배운 것**: 이 drift는 **조용히 일어납니다.** 마이그레이션은 성공하고 테스트도 대부분 통과합니다(앱 2계층이 여전히 막으니까). 3계층만 소리 없이 사라집니다. **마이그레이션 파일은 생성물이 아니라 리뷰 대상 코드**라는 걸 배웠습니다. 이건 Prisma만의 문제가 아니라 모든 마이그레이션 도구의 공통 성질입니다.

---

### T3. ★Docker에서만 `npm ci`가 실패 (npm 10 vs 11)

> **S**: 호스트에서는 `npm ci`가 되는데 `node:22-alpine` 이미지 안에서만 **EUSAGE**로 실패했습니다. 락파일 정합성 문제라는데 로컬 재현이 안 됐습니다.
>
> **T**: "왜 로컬은 되는데 Docker만 안 되는가"를 규명해야 했습니다.
>
> **A**: 단계적으로 좁혔습니다.
>
> 1. 에러가 가리킨 건 `@emnapi/*` — jest-resolve가 끌어오는 **wasm32-wasi 바인딩**이었습니다.
> 2. 이건 **optionalDependencies 서브트리**이고 **어떤 플랫폼에서도 실제 설치되지 않는** 노드였습니다.
> 3. `package.json`에 `overrides`를 넣어봤는데 **npm 10에서는 여전히 실패하고 npm 11에서는 통과**했습니다.
> 4. 확인해보니 `node:22-alpine` 내장 npm이 **10.9.8**, 호스트는 **11.6.2**였습니다. npm 10의 락파일 정합성 검사가 **도달 불가능한 optional 서브트리를 오판**하는 것이었습니다.
>
> 이미지 안의 npm을 호스트와 같은 메이저로 올려서 **검증 로직 자체를 맞췄습니다.**
>
> ```dockerfile
> RUN npm install -g npm@11.6.2
> ```
>
> Dockerfile 주석에 **원인 분석과 실측 버전 번호까지** 남겼습니다.
>
> **R**: Docker 빌드 성공, 검수자가 프로덕션 이미지를 실제로 띄워 `/health` 200까지 확인했습니다.
>
> **배운 것**: ① **"로컬은 되는데 CI/Docker만 안 된다"면 런타임 버전 차이를 먼저 의심합니다.** Node 버전은 다들 고정하는데 **npm 버전은 이미지에 딸려오는 걸 그냥 씁니다** — 여기가 사각지대였습니다. ② `overrides`처럼 '고쳐질 것 같은' 처방을 시도했는데 여전히 실패하면, **처방이 아니라 진단을 다시 해야 합니다.** ③ 주석에 실측 버전을 남기면 나중에 npm이 고쳐졌을 때 이 라인을 지워도 되는지 판단할 근거가 됩니다.

---

### T4. ★CORS가 프로덕션에서 임의 오리진을 반사 허용

> **S**: 배포 전 검수에서 검수자가 실제로 `curl -H "Origin: https://evil.example.com"`을 날려봤습니다.
>
> **T**: 응답에 `Access-Control-Allow-Origin: https://evil.example.com`이 **그대로 반사**됐습니다.
>
> **A**: 원인은 한 줄이었습니다.
>
> ```ts
> origin: process.env.CORS_ORIGINS?.split(",") ?? true; // 값이 없으면 true = 전체 허용
> ```
>
> 그리고 `render.yaml`에 `CORS_ORIGINS`가 아예 없었습니다. **"설정을 깜빡하면 열리는"** 방향으로 실패하도록 만들어져 있었습니다.
>
> **영향을 정직하게 평가**했습니다. 토큰이 쿠키가 아니라 헤더 + localStorage라 **즉시 계정 탈취로 이어지진 않습니다**(악성 사이트가 다른 오리진의 localStorage를 못 읽으니까요). 하지만 공개 API를 아무 사이트에서나 브라우저로 호출 가능한 상태로 배포되는 겁니다.
>
> **fail-fast**로 고쳤습니다.
>
> ```ts
> if (process.env.NODE_ENV === "production" && origins.length === 0)
>   throw new Error("CORS_ORIGINS 환경변수가 비어 있습니다. ...");
> ```
>
> 그리고 **REST와 WebSocket이 같은 함수를 쓰게** 합쳤습니다 — 각자 fallback을 두면 한쪽만 잠그는 사고가 납니다. 왜 프로덕션에서만 엄격한지도 주석에 남겼습니다(로컬은 프론트 포트가 수시로 바뀌고 위협 모델이 없습니다).
>
> **R**: `render.yaml`에 `CORS_ORIGINS`를 추가하고 배포된 Vercel 도메인으로 설정했습니다. 이제 값이 없으면 **서버가 아예 안 뜹니다.**
>
> **배운 것**: **보안 설정의 기본값은 '닫힘'이어야 하고, 설정이 없을 때 '조용히 열리는' 것이 가장 나쁩니다.** "일단 열어두고 나중에 잠그기"는 나중이 오지 않습니다. 그리고 fail-fast는 **배포 시점에 실패하는 게 운영 중에 뚫리는 것보다 낫다**는 판단입니다.

---

### T5. ★Render 리전 장애를 진단하고 우회

> **S**: 배포 중 특정 리전(Singapore)에서 **새 배포가 진행되지 않는** 상황을 만났습니다. 증상만 보면 제 설정 문제로 보였습니다.
>
> **T**: 계속 코드/설정을 고칠 것인가, 다른 가능성을 확인할 것인가를 결정해야 했습니다.
>
> **A**: **플랫폼 상태를 먼저 확인**했습니다. Render 상태 페이지에서 해당 리전의 인시던트를 확인했고, 원인이 제 쪽이 아니라는 게 명확해졌습니다.
>
> 그러자 선택지가 둘로 정리됐습니다 — **(a) 복구를 기다린다 (b) 다른 리전에 서비스를 생성해 우회한다.** 복구 시점을 통제할 수 없고 배포 일정이 있어서 **(b)** 를 택했습니다.
>
> **R**: 다른 리전으로 배포를 완료했고 라이브 서비스가 정상 동작합니다.
>
> **배운 것**: 여기서 실제 역량은 '리전을 바꿨다'가 아니라 셋입니다.
>
> - **증상만 보고 원인을 단정하지 않은 것.** '배포가 안 된다'는 제 설정 오류일 수도, 플랫폼 장애일 수도 있습니다. 후자를 확인하는 비용(상태 페이지 1분)이 전자를 파고드는 비용(수십 분)보다 훨씬 쌉니다.
> - **외부 의존성의 관측 창구를 아는 것.** 클라우드/PaaS는 상태 페이지·인시던트 히스토리를 공개합니다. 이걸 **디버깅 도구의 일부**로 쓰는 습관입니다.
> - **'기다린다 vs 우회한다'를 비용으로 판단한 것.** 통제할 수 없는 변수 앞에서 통제 가능한 변수를 바꿔 진행했습니다.
>
> ⚠️ **정확성 주의**: 저장소의 `render.yaml`은 아직 `region: singapore`로 선언돼 있습니다. Blueprint 값과 실제 생성된 서비스의 리전이 다를 수 있으니, 면접 전에 대시보드에서 실제 리전을 확인하고 말하는 게 안전합니다. 그리고 **리전 선택 자체에도 근거**가 있다는 걸 덧붙이면 좋습니다 — 백엔드와 DB(Neon) 리전을 맞추면 **서버↔DB 왕복이 요청당 여러 번 누적**되는 걸 줄일 수 있습니다(사용자↔서버 지연은 요청당 1회).

---

## 10. 배포 / 인프라

### Q10-1. 배포 구성을 설명해주세요.

**답변**

> "**프론트는 Vercel, 백엔드는 Render에 Docker로, DB는 Neon Postgres**입니다.
>
> 백엔드 이미지는 **멀티스테이지**입니다. builder에서 `npm ci` → `npm run build` → `npm prune --omit=dev`로 프로덕션 의존성만 남기고, runner 스테이지에 `node_modules`·`dist`·`prisma`만 복사합니다.
>
> 두 가지 디테일이 있습니다.
>
> - **`package*.json`을 먼저 복사하고 `npm ci`** → 소스만 바뀌면 의존성 레이어가 캐시됩니다.
> - **`prisma/`를 package.json과 함께 먼저 복사**해야 합니다. `postinstall`이 `prisma generate`인데 스키마가 없으면 실패하니까요.
>
> 컨테이너 기동 시 **`prisma migrate deploy`를 먼저 실행**하고 앱을 띄웁니다. 중요한 건 **테스트도 배포와 똑같은 `migrate deploy`를 쓴다**는 겁니다 — Testcontainers 헬퍼가 그 명령을 그대로 실행합니다. '테스트와 프로덕션의 스키마 적용 방식이 다르다'는 흔한 함정을 피했습니다."

---

### Q10-2. `DATABASE_URL`과 `DIRECT_URL`을 왜 나눴나요?

**답변**

> "**앱 런타임은 커넥션 풀러, 마이그레이션은 직결**을 씁니다.
>
> - `DATABASE_URL` — Neon 풀러(PgBouncer류). 서버리스 환경에서 커넥션 수를 아껴 씁니다.
> - `DIRECT_URL` — Neon 직결(non-pooled). **DDL은 풀러 뒤에서 돌리면 안 됩니다.** 트랜잭션 풀링 모드는 세션 상태나 어드바이저리 락을 보장하지 않아서 마이그레이션이 깨질 수 있습니다.
>
> 그런데 **여기서 배포 블로커가 하나 나왔습니다.** `render.yaml`에 `DIRECT_URL`이 없어서, 검수자가 프로덕션 이미지를 띄워보니 이렇게 죽었습니다.
>
> ```
> Error: The datasource.url property is required in your Prisma config file when using prisma migrate deploy.
> ```
>
> Dockerfile CMD가 `migrate deploy`를 먼저 하는데 `prisma.config.ts`가 `DIRECT_URL`만 읽거든요. Render에서 **크래시 루프**가 났을 상황이었습니다.
>
> 고치면서 **각 환경변수 옆에 '없으면 무슨 일이 나는지'를 주석으로** 달았습니다.
>
> ```yaml
> - key: DIRECT_URL # Neon 직결 — prisma.config.ts가 migrate deploy에 사용, 없으면 기동 즉시 종료
> - key: CORS_ORIGINS # 배포 후 확정된 Vercel 도메인 — 없으면 main.ts가 fail-fast
> ```
>
> 그리고 `PORT`는 **일부러 넣지 않았습니다** — Render가 자동 주입하는 값이라 고정하면 오히려 충돌합니다. 그것도 주석으로 남겼습니다.
>
> **배운 것은 '환경변수는 목록이 아니라 계약'** 이라는 겁니다. 각 변수가 없을 때 어떤 실패가 나는지가 문서에 없으면 배포 담당자는 그냥 빠뜨립니다."

---

### Q10-3. 프로덕션 시드는 어떻게 하나요?

**답변**

> "**로컬 머신에서 Neon을 직접 겨냥해 1회 실행**합니다.
>
> ```bash
> cd backend
> DATABASE_URL='<neon-pooled>' DIRECT_URL='<neon-direct>' npm run seed:demo
> ```
>
> 이것도 검수에서 나온 블로커였습니다. 마이그레이션만 적용된 빈 DB에 `admin/admin`으로 로그인하면 **401**이 났습니다 — 마이그레이션은 **스키마**를 만들지 **계정**을 만들지 않으니까요. 즉 '회원가입 없이 둘러보기' 버튼이 라이브에서 동작하지 않는 상태였습니다.
>
> 컨테이너 안에서 시드를 돌릴 수도 없었습니다. 프로덕션 이미지가 `npm prune --omit=dev`로 **`ts-node`를 지웠고**, Render 무료 플랜엔 **셸 접근이 없습니다.**
>
> 대안(CMD를 `migrate deploy && seed:demo && node dist/main.js`로 바꾸기)도 검토했는데, 그러려면 **시드를 빌드 산출물(`dist`)로 함께 컴파일**해야 합니다. 지금 규모에선 로컬 1회 실행이 더 싸다고 판단하고, 그 절차와 근거를 README에 명시했습니다. 시드는 **idempotent**(admin이 이미 있으면 조기 종료)라 재실행이 안전합니다.
>
> **배운 것**: **'로컬에서 되는 절차'와 '프로덕션에서 가능한 절차'는 다릅니다.** 프리티어 PaaS는 셸이 없고 이미지가 최소화돼 있어서, 로컬에서 당연한 스크립트가 아예 못 돕니다. **배포 절차는 실제 환경에서 한 번 해보기 전까지 검증되지 않은 것**입니다."

---

### Q10-4. Vite 환경변수 관련해서 주의할 점이 있나요?

**답변**

> "**Vite는 빌드 시점에 `import.meta.env`를 리터럴로 치환합니다.**
>
> 그래서 `VITE_API_URL`·`VITE_WS_URL`을 **Vercel 프로젝트 환경변수로 빌드 전에** 등록해야 합니다. 배포 후 대시보드에서 값을 바꿔도 **이미 만들어진 번들에는 반영되지 않고, 다시 빌드해야** 합니다.
>
> 백엔드는 `process.env`를 런타임에 읽으니 다릅니다. **같은 '환경변수'라는 이름인데 동작 시점이 완전히 다르다**는 걸 구분해야 합니다.
>
> 이 '빌드타임 vs 런타임 변수' 축은 제가 다른 프로젝트에서도 반복해서 만난 함정입니다. 어떤 프로젝트에서는 **빌드 단계에 런타임 시크릿이 없어서** 실패했고, 다른 프로젝트에서는 **빌드에만 있던 값이 다음 빌드에서 사라져서** 실패했습니다. 같은 축의 반대 방향 사례라 하나의 서사로 묶어서 말할 수 있습니다."

---

### Q10-5. 헬스체크와 콜드스타트는요?

**답변**

> "`GET /health`가 **앱 상태 + `SELECT 1` DB ping**을 확인합니다. Render의 `healthCheckPath`로 등록돼 있고, 전역 prefix `/api`에서 **제외**했습니다(`app.setGlobalPrefix('api', { exclude: ['health'] })`).
>
> DB ping을 포함한 이유는, **앱 프로세스는 살아 있는데 DB 연결이 끊긴 상태**를 '정상'으로 보고하면 헬스체크의 의미가 없기 때문입니다.
>
> **콜드스타트는 숨기지 않고 README에 명시**했습니다. Render 무료 플랜은 15분 무활동 시 슬립이라 첫 요청이 수십 초 걸릴 수 있습니다. 데모 전 `/health`를 한 번 열어 깨워두는 걸 권장한다고 적었습니다.
>
> 개선안도 알고 있습니다 — 유료 티어, 외부 cron 핑(다만 무료 플랜 750시간 한도와 트레이드오프가 있습니다), 또는 콜드스타트가 짧은 런타임으로 이전. 지금은 포트폴리오라 **'있는 그대로 알리고 대응법을 안내하는 것'** 이 맞다고 판단했습니다."

---

## 11. 테스트 / 품질 / 검수

### Q11-1. 테스트 전략을 설명해주세요.

**답변**

> "**단위 22 + Testcontainers 통합 113 = 135개**입니다. 나누는 기준이 명확합니다.
>
> **단위테스트**는 **순수 함수와 순수 판단**만 봅니다 — 트리 규칙(최대 깊이·순환), `diffFields`, 로그인 스키마 검증, CORS 설정 해석, `AuthService`의 bcrypt 분기. DB가 필요 없는 것들입니다.
>
> **통합테스트**는 **실제 Postgres 컨테이너 + 실제 HTTP + 실제 소켓**으로 돕니다. 이 프로젝트가 증명하려는 것들이 대부분 **DB에서만 성립**하기 때문입니다.
>
> | 검증 대상                          | 목으로 가능?                        |
> | ---------------------------------- | ----------------------------------- |
> | 복합 FK가 타 조직 부모 참조를 거부 | ❌ 제약은 DB에만 있다               |
> | 트랜잭션 롤백 시 감사로그도 없음   | ❌ 트랜잭션은 DB 엔진의 것          |
> | 카운터 원자 증감                   | ❌ `{increment:1}`은 SQL로 번역된다 |
> | 실제 SQL 왕복 횟수                 | ❌ 목은 쿼리를 안 낸다              |
> | 소켓 이벤트가 실제로 도착/미도착   | ❌ 실제 연결이 필요                 |
>
> 시나리오는 PLAN 문서에 **T-1 ~ T-26으로 번호를 붙여** 미리 정의했고, 그룹이 넷입니다 — 테넌트 격리 / RBAC 경계 / 실행·실시간·동시성 / 감사로그·인증.
>
> **격리 방식**은 스펙 파일마다 **독립 컨테이너**입니다. 속도보다 격리를 우선했습니다. 그리고 스키마는 **배포와 동일한 `prisma migrate deploy`** 로 적용합니다."

---

### Q11-2. 인상적인 테스트 하나만 꼽는다면?

**답변**

> "**T-16(트랜잭션 롤백 시 이벤트 미발행)** 입니다. 세 가지 기법이 한 테스트에 들어갑니다.
>
> **① 실패 주입** — `moduleRef.get(AuditService)`로 DI 컨테이너에서 실제 프로바이더를 꺼내 `mockRejectedValueOnce`로 실패를 주입합니다. 트랜잭션 **도중** 실패를 인위적으로 만드는 방법입니다.
>
> **② '일어나지 않음'의 검증** — `assertNoEvent(socket, 'run:case.recorded', 1000)`. 부정 검증이라 타임아웃 기반일 수밖에 없습니다. `Promise.all`로 REST 요청과 동시에 돌려서, 요청이 처리되는 동안 이벤트가 안 오는 걸 확인합니다.
>
> **③ 세 가지 동시 검증** — DB 롤백(`result === 'PENDING'`), 카운터 롤백(`failedCount === 0`), 이벤트 미발행.
>
> 이 테스트가 **아키텍처 문서의 주장('emit은 커밋 이후에만')을 실행 가능한 증거로 바꿔줍니다.** 문서에 원칙을 적는 것과 그 원칙이 지켜지는 걸 증명하는 건 다른 일입니다."

---

### Q11-3. ★배포 전 검수에서 무엇이 나왔나요?

**답변** — 프로세스 자체가 어필 포인트.

> "**블로커 5건**이 나와서 전부 고친 뒤 배포했습니다.
>
> 1. 결과 재기록 시 카운터 이중 증가
> 2. `render.yaml`에 `DIRECT_URL` 누락 → 컨테이너 기동 즉시 사망
> 3. CORS가 프로덕션에서 임의 오리진 반사 허용
> 4. 프로덕션 시드 절차 부재 → 데모 로그인 401
> 5. 초대 링크가 데드링크(백엔드 API는 있는데 프론트 라우트가 없음)
>
> **공통점이 있습니다. 전부 CI가 그린인 상태에서 나왔고, 전부 '경계'에서 났습니다.**
>
> | 블로커        | CI가 잡을 수 있었나 | 왜 못 잡나                                                 |
> | ------------- | ------------------- | ---------------------------------------------------------- |
> | 카운터        | ❌                  | 그 케이스의 테스트가 아예 없었다                           |
> | `DIRECT_URL`  | ❌                  | 테스트는 env를 직접 주입한다. **배포 매니페스트는 미검증** |
> | CORS          | ❌                  | 프로덕션 env 조합에서만 발현                               |
> | 시드 절차     | ❌                  | 운영 **절차**의 부재라 코드에 흔적이 없다                  |
> | 초대 데드링크 | ❌                  | 백엔드·프론트 각각은 옳다. **연결이 없다**                 |
>
> **단위 테스트는 경계 안쪽을 지키고, 사람 검수는 경계를 넘어가 봅니다.** 특히 5번은 백엔드 테스트도 그린, 프론트 빌드도 그린인데 **유저 스토리를 끝까지 따라가 봐야만** 나옵니다."

**꼬리질문: "검수를 어떻게 했나요?"**

> "**구현 보고를 신뢰하지 않고 직접 실행**하는 방식이었습니다. 검수 문서 머리말에 그 원칙을 적어두고, **직접 실행해 확인한 사실** 표를 맨 앞에 뒀습니다.
>
> - `npm test` 실측 — unit 22/22, e2e 113/113
> - **프로덕션 Docker 이미지를 실제로 띄워서** 임시 Postgres에 붙이고 `/health` 200까지 확인
> - `DIRECT_URL`만 제거하고 다시 띄워서 어떻게 죽는지 확인
> - `curl -H "Origin: https://evil.example.com"`으로 CORS 반사 실측
> - 빈 DB에서 데모 로그인 시도 → 401 확인
> - **카운터 버그는 임시 e2e 스펙을 새로 작성해서 재현**하고, 검수 후 그 스펙은 지우고 정식 회귀 테스트로 편입
>
> 마지막 항목이 특히 중요합니다. **코드를 읽고 의심한 것으로 끝내지 않고 실제 수치(`total:1, passed:2`)를 확보**했습니다. 그래야 '고쳐야 한다'가 논쟁이 아니라 사실이 됩니다.
>
> 그리고 **수정 계획도 엔지니어링**했습니다. 블로커별 서브시스템과 파일 충돌을 표로 정리해서, 🔴-2와 🔴-3이 같은 `render.yaml`을 건드리니 한 작업으로 묶고 나머지는 병렬로 처리했습니다."

**꼬리질문: "잘된 점은 어떻게 확인했나요?"**

> "칭찬 항목에도 **파일:라인 근거**를 붙였습니다. 예를 들어 '멀티테넌시 3계층이 문서상 주장이 아니라 실제로 구현돼 있다'는 항목에 가드·확장·마이그레이션 SQL의 위치를 각각 적고 **'세 계층 모두 코드로 확인했다'** 고 명시했습니다.
>
> **설계 문서의 주장과 실제 코드를 대조하는 것**이 검수의 본질이라고 봅니다. 문서에 '3계층으로 막는다'고 써놓고 실제론 1계층만 있는 경우가 흔하니까요."

---

### Q11-4. 테스트를 먼저 썼나요?

**답변**

> "청크 단위로 **'테스트 먼저 → 구현 → 리팩터'** 순서를 계획에 명시했고, 대체로 지켰습니다. 특히 **테넌시 격리(T-1~~T-7)와 RBAC 매트릭스(T-8~~T-13)** 는 빨간 상태로 먼저 작성했습니다.
>
> 이게 실제로 도움이 됐습니다. '조직 A 사용자가 조직 B의 orgId로 모든 리소스에 접근 → 전부 404'라는 테스트를 먼저 써두면, **구현이 그 형태를 강제**합니다. 404가 아니라 403을 던지고 싶은 유혹이 사라지고, 리소스 유형별로 빠뜨린 게 있으면 바로 드러납니다.
>
> ⚠️ **정직하게 말하면 전부 TDD는 아니었습니다.** 프론트 화면이나 시드 스크립트처럼 '먼저 써도 별 이득이 없는' 부분은 구현 후 검증했습니다. TDD를 교조적으로 지키는 것보다 **'무엇을 테스트 먼저 쓸 가치가 있는가'를 판단하는 것**이 중요하다고 생각합니다. 이 프로젝트에선 격리·권한·트랜잭션 경계가 그 대상이었습니다."

---

## 12. 프론트엔드

### Q12-1. 조직 전환 시 이전 조직 데이터가 보이는 문제는 어떻게 막았나요?

**답변**

> "**쿼리 키 선두에 `orgId`를 고정**했습니다.
>
> ```ts
> export function orgScopedKey(orgId, ...rest) {
>   return ["orgs", orgId ?? "no-org", ...rest] as const;
> }
> ```
>
> TanStack Query는 키가 다르면 다른 캐시 엔트리입니다. 그래서 조직을 전환하면 키가 통째로 바뀌어 **이전 조직 데이터를 재사용할 수 없습니다.**
>
> 이걸 안 하면 어떻게 되냐면, 조직 전환 직후 화면이 **이전 조직의 캐시를 그대로 보여줍니다.** 서버가 아무리 완벽히 격리해도 **브라우저에서 다른 회사 데이터가 한 프레임 노출**되는 사고입니다.
>
> 중요한 건 **'조심하기'가 아니라 '통로를 하나로 좁힌 것'** 입니다. 조직 스코프 키는 반드시 이 헬퍼로만 만들도록 하고, **이유를 파일 주석에 남겼습니다.** 제가 이 프로젝트 전체에서 반복한 패턴 — 규칙을 문서가 아니라 구조로 강제하기 — 의 프론트 버전입니다."

---

### Q12-2. 실시간 이벤트를 캐시에 어떻게 반영하나요?

**답변**

> "**`setQueryData`로 부분 패치**합니다. `invalidateQueries`(전체 재조회)는 재연결 때만 씁니다.
>
> ```ts
> function onCaseRecorded(payload) {
>   patchRunCaseResult(queryClient, orgId, runId, { id: payload.runCaseId, result: payload.result, ... });
>   if (payload.recordedBy.id !== currentUserId)
>     showToast(`${payload.recordedBy.name}님이 방금 ${payload.result} 기록`, 'info');
> }
> ```
>
> 500개짜리 실행에서 결과 하나 바뀔 때마다 전체를 다시 받으면 실시간의 의미가 없습니다. 이벤트 페이로드에 필요한 정보가 다 들어 있으니 해당 행만 갱신합니다.
>
> `recordedBy.id !== currentUserId` 조건으로 **내가 한 기록에는 토스트를 안 띄우는 것**도 디테일입니다. 자기가 누른 걸 알려주는 알림은 소음입니다.
>
> 소켓 연결은 **싱글턴**입니다. 화면마다 별도 연결을 열면 서버 커넥션만 늘고 룸 상태를 공유할 수 없어서, **연결 하나 위에 룸을 여러 개** 올리고 각 훅이 필요한 룸만 join/leave 합니다."

---

### Q12-3. 프론트에서 아쉬운 점이 있나요?

**답변**

> "**번들이 단일 청크 870kB(gzip 259kB)** 입니다. Recharts가 큰 몫입니다.
>
> 라우트 단위 `React.lazy` 코드 스플리팅을 넣으면 첫 로드 체감이 개선됩니다. 특히 **차트가 있는 대시보드와 감사로그 화면을 분리**하면, 로그인 화면 진입 시 Recharts를 안 받아도 됩니다.
>
> 지금 안 넣은 이유는 우선순위입니다 — 검수에서 🟡(배포를 막지 않음)로 분류됐고, 블로커 5건을 먼저 처리해야 했습니다. **다만 이건 '몰라서 안 한 것'이 아니라 '순서상 뒤로 민 것'** 이고, 개선 목록에 있습니다.
>
> 그리고 하나 더 — **프론트 컴포넌트 테스트가 없습니다.** 백엔드는 135개인데 프론트는 빌드/타입체크만 돕니다. 실시간 훅(`useRunSocket`)의 재연결 로직이나 axios 인터셉터의 refresh 큐잉은 **테스트할 가치가 충분한 로직**인데 안 잠겨 있습니다."

---

## 13. ★ 압박 질문 모음

### Q13-1. "3계층이 과한 거 아닌가요? 하나만 잘 만들면 되잖아요."

> "각 계층이 막는 실패의 **종류가 다릅니다.**
>
> 1계층만 있으면 서비스에서 `where`를 빠뜨린 쿼리 하나가 전 조직을 반환합니다. 2계층만 있으면 비멤버가 임의 orgId로 접근합니다. 3계층만 있으면 조회 자체는 못 막습니다.
>
> 그리고 판단 기준을 **'지금 코드 양'이 아니라 '실수의 비용'** 으로 잡았습니다. 조직 데이터 유출은 되돌릴 수 없고 이 도메인에선 계약 위반입니다. 반면 확장 코드는 파일 하나 100줄이고 서비스 코드엔 흔적이 없습니다. **비용이 비대칭이면 확률로 판단하면 안 된다**고 생각했습니다.
>
> 반대로 **과설계라고 판단해서 뺀 것도 있습니다** — RLS, Redis adapter, 낙관적 잠금. '무조건 겹겹이'가 아니라 **항목마다 비용/이득을 따졌다**는 게 답변의 핵심입니다."

---

### Q13-2. "요청마다 DB 조회하는 건 성능 문제 아닌가요?"

> "인덱스를 정확히 타는 **PK 룩업 수준**의 조회 1회입니다. 요청당 이미 여러 쿼리가 나가는 상황에서 +1은 비율상 미미합니다.
>
> 그리고 **얻는 것이 명확합니다** — 강등·추방이 다음 요청부터 즉시 반영됩니다. 토큰에 role을 박으면 그게 15분 지연됩니다. 외주 인력이 섞이는 도메인에서 그 15분은 실제 보안 결함입니다.
>
> 병목이 되면 **짧은 TTL 캐시**로 최적화할 수 있고, 그때도 반영 지연의 상한을 제가 정합니다(토큰 만료는 못 정합니다). 다만 **측정 없이 캐시를 넣으면 캐시 무효화 문제를 공짜로 얻는 것**이라, 지금은 안 넣는 게 맞다고 봅니다."

---

### Q13-3. "테스트 135개면 많아 보이는데, 정말 의미 있나요?"

> "숫자보다 **무엇을 테스트했느냐**가 중요하다고 생각합니다.
>
> 이 프로젝트 테스트의 대부분은 **'구현이 이렇게 생겼다'가 아니라 '이 불변식이 깨지지 않는다'** 를 검증합니다. 조직 경계를 넘을 수 없다, 강등이 즉시 반영된다, 롤백되면 이벤트가 안 나간다, 카운터 합이 total을 넘지 않는다 같은 것들입니다.
>
> 그리고 **목으로는 증명 불가능한 것**만 통합테스트로 뺐습니다. 복합 FK 거부, 트랜잭션 롤백, 원자적 증감, 실제 소켓 도착 여부 — 전부 실제 DB/연결이 아니면 의미가 없습니다.
>
> ⚠️ **다만 부족한 것도 정직하게 말씀드리면**, 프론트 테스트가 0개입니다. 그리고 **테스트가 그린이어도 버그가 있을 수 있다**는 걸 이번에 카운터 버그로 정확히 배웠습니다 — 기존 테스트가 자기 전이를 안 봐서 통과시켰거든요."

---

### Q13-4. "설계 문서를 그렇게 많이 쓰는 게 비효율 아닌가요?"

> "**오히려 시간을 줄였다**고 생각합니다. 근거가 셋입니다.
>
> **① 비범위 표가 스코프 크리프를 막았습니다.** 파일 첨부, 이메일 발송, Jira 연동 같은 게 계속 떠오르는데, 그때마다 '이게 이 프로젝트가 증명하려는 것에 기여하는가'를 표로 판단했습니다. 판단을 매번 새로 하지 않아도 됩니다.
>
> **② 기각한 대안을 미리 적어두니 나중에 재논쟁이 없었습니다.** RLS를 왜 안 쓰는지 ARCHITECTURE에 근거와 함께 적어두니, 구현 중에 다시 흔들리지 않았습니다.
>
> **③ 검수의 기준이 됐습니다.** 문서가 없으면 '이게 의도인가 버그인가'를 판단할 수 없습니다. 실제로 검수는 **'설계 문서와 코드를 대조'** 하는 방식이었고, 그래서 '3계층이 실제로 구현돼 있다'를 확인할 수 있었습니다.
>
> ⚠️ 다만 **문서와 코드가 어긋나는 문제**도 실제로 있었습니다. API 문서에 `AUTH_TOKEN_EXPIRED` 에러 코드가 있는데 실제로는 반환되지 않고, WS `error` 이벤트도 문서에만 있습니다. **문서를 쓰는 것과 유지하는 건 다른 일**이고, 이건 제 숙제입니다."

---

### Q13-5. "혼자 만든 프로젝트인데 팀에서 협업할 때도 이렇게 하나요?"

> "**오히려 팀에서 더 중요하다고 생각합니다.**
>
> 이 프로젝트의 핵심 설계는 전부 **'다음 사람이 실수하지 않게 하는 것'** 입니다. 혼자 만들면 제가 규칙을 다 기억하니까 사실 필요 없습니다. Prisma 확장도, 브랜드 타입도, 쿼리 키 헬퍼도 **'새 기능을 추가하는 사람이 조직 필터를 빠뜨려도 안전한가'** 를 전제로 만든 것입니다.
>
> 그리고 팀 협업에서 실제로 유효할 것들이 있습니다.
>
> - **코드 주석 규칙** — 자명한 주석은 금지하고, 트랜잭션 경계·보안 규칙·비자명 로직에만 '왜'를 남깁니다. 특히 **버그를 겪고 고친 자리에는 그 사실을 적었습니다**(refresh 폐기 트랜잭션, Dockerfile npm 버전).
> - **Conventional Commits + 의미 단위 커밋** — 히스토리가 곧 설명이 됩니다.
> - **정당한 예외를 세어 두기** — 원본 Prisma를 쓰는 4곳에 각각 이유를 적어두면, 리뷰어가 '5번째 예외'를 발견했을 때 의심할 수 있습니다.
>
> 반대로 팀이면 달라질 것도 있습니다. 브랜드 타입의 캐스팅 우회는 **ESLint 커스텀 룰**로 잠가야 하고, 검수도 저 혼자가 아니라 PR 리뷰 프로세스로 들어가야 합니다."

---

### Q13-6. "이 프로젝트에서 다시 한다면 뭘 바꾸겠어요?"

> "세 가지입니다.
>
> **① 커서 페이지네이션에 타이브레이커를 처음부터 넣겠습니다.** `orderBy: createdAt` + `cursor: id`는 지금 다섯 군데에 똑같이 있습니다. **같은 패턴을 복사할 때 결함도 함께 복사됩니다.** 첫 구현에서 공통 헬퍼로 만들었으면 한 곳만 고치면 됐을 겁니다.
>
> **② 카운터 회귀 테스트를 처음부터 자기 전이 포함으로 짜겠습니다.** '상태 전이 테스트는 X→Y뿐 아니라 X→X도 본다'를 체크리스트에 넣겠습니다.
>
> **③ 배포 매니페스트를 코드 초기부터 검증 대상에 넣겠습니다.** 블로커 5건 중 2건이 `render.yaml` 관련이었는데, 이건 **코드 리뷰 대상이 아니라고 무의식적으로 취급**했던 게 원인입니다. `.env.example`과 `render.yaml`이 실제로 동기화되는지 확인하는 절차가 없었습니다.
>
> 반대로 **다시 해도 똑같이 할 것**은 3계층 격리와 커밋 후 emit입니다. 둘 다 '나중에 넣기'가 매우 어려운 종류의 설계라, 처음부터 잡은 게 맞았습니다."

---

### Q13-7. "가장 자신 없는 부분은?"

> "**수평 확장 경험이 없다**는 겁니다.
>
> 이 프로젝트는 인스턴스 1대 전제로 설계했고, 프레즌스도 인메모리입니다. Redis adapter가 왜 필요한지, 어디를 바꿔야 하는지는 문서로 정리했지만 **실제로 여러 인스턴스를 띄워서 룸 브로드캐스트가 깨지는 걸 겪어본 적은 없습니다.**
>
> 마찬가지로 **부하 상황에서의 판단**도 경험이 부족합니다. 인덱스 설계나 쿼리 예산은 '이론적으로 이게 맞다'까지는 했지만, 실제 트래픽에서 `EXPLAIN ANALYZE`를 보고 튜닝해본 경험은 없습니다.
>
> 그래서 이 프로젝트에서는 **'측정할 수 있는 것만 주장한다'** 는 원칙을 지키려 했습니다. 쿼리 수는 실제로 세는 유틸을 만들어 테스트로 고정했고, 실시간 지연 목표(p95 500ms)도 측정 가능한 형태로 적었습니다. 측정 없이 '빠르다'고 말하지 않으려 했습니다."

---

## 14. 내가 아는 한계 (먼저 말하기)

면접에서 **먼저 꺼내면 강점**이 되는 목록. "검수에서 🟡로 분류돼 배포는 막지 않았지만 고칠 목록입니다"로 시작한다.

### 코드/설계

1. **커서 페이지네이션 타이브레이커 없음** — `orderBy: createdAt` + `cursor: id`. 같은 타임스탬프가 여럿이면(한 트랜잭션에서 감사로그 여러 건 생길 때 **실제 발생**) 경계에서 중복/누락 가능. 정답은 `[{createdAt:'desc'},{id:'desc'}]`.
2. **버그 담당자에 조직 밖 사용자 지정 가능** — 실행 배정자는 `assertAssigneesAreMembers`로 막는데 **버그만 비대칭**. 같은 검증 재사용이 정답.
3. **감사로그 metadata에 `Object.keys(dto)` 사용** — 지금은 DTO가 화이트리스트라 안전하지만 **민감 필드가 늘면 새는 구조**.
4. **실행 생성 시 `suiteIds`가 하위 스위트를 포함하지 않음** — 직속 케이스만 조회한다. 주석/문서와 실제 동작이 어긋난 상태.
5. **브랜드 타입은 의도적 캐스팅으로 뚫린다** — TS 구조적 타이핑의 한계. ESLint 커스텀 룰이 다음 단계.
6. **로그인 브루트포스 방어 없음** — 공개 데모라 `admin/admin`이 알려진 상태. `@nestjs/throttler`를 로그인 라우트 한정 적용이 정답.
7. **로그인 타이밍 사이드 채널** — 계정이 없으면 bcrypt를 건너뛰어 응답이 빠르다. 더미 해시 비교가 표준 대응.
8. **미사용 의존성** — `class-validator`, `class-transformer`가 남아 있다.
9. **API 문서와 구현 불일치 3건** — `AUTH_TOKEN_EXPIRED` 미반환 / WS `error` 이벤트 미구현 / `run:progress.updated` 발생 시점.
10. **CI의 lint가 `--fix`** — 자동 수정 후 통과해서 위반을 못 잡는다.
11. **카운터 복구 경로 없음** — `recount` 엔드포인트가 비범위. 인덱스(`test_run_cases(testRunId, result)`)는 준비돼 있다.

### 프론트

12. **단일 청크 870kB** — 라우트 `React.lazy` 분할 미적용(Recharts가 큰 몫).
13. **프론트 테스트 0개** — `useRunSocket` 재연결 로직, axios refresh 큐잉은 테스트 가치가 충분한데 안 잠겼다.

### 명시적 비범위 (의도적)

- **Postgres RLS** — 향후 과제 1순위. 앱 3계층으로 대체.
- **WebSocket 수평 확장(Redis adapter)** — 인스턴스 1대 전제. 확장 지점만 명시.
- **낙관적 잠금** — LWW + "누가 방금 덮었는지" 실시간 표시로 대체.
- **파일 첨부 / 이메일 발송 / OAuth / Jira 연동 / CSV** — 증명 대상(멀티테넌시·RBAC·실시간·감사)에 기여가 없거나 다른 유형 태그.

### 운영

- **Render 무료 15분 슬립** → 콜드스타트 수십 초. README에 명시 + `/health` 워밍업 안내.
- **RefreshToken 정리 배치 없음** / **감사로그 파티셔닝 없음**.
- **데모 GIF 미첨부** — 실시간 2브라우저 동기화 GIF가 이 프로젝트의 가장 강한 증거인데 아직 없다.

---

## 15. 역질문 / 마무리

### 좋은 역질문 (기술 관심을 보여주는 것)

1. "팀에서 멀티테넌시를 다루신다면, 애플리케이션 계층과 DB 계층 중 어디에 무게를 두시나요? RLS를 실제로 운영하시는지 궁금합니다."
2. "실시간 기능이 있는 서비스라면, 이벤트 유실 복구를 어떻게 처리하시나요? 저는 재연결 시 REST 재조회로 했는데 규모가 커지면 다른 방법이 필요할 것 같습니다."
3. "감사로그를 도메인 트랜잭션 안에 넣으면 쓰기 부하가 늘어나는데, 규모가 커지면 어느 시점에 비동기로 분리하시나요?"
4. "코드 리뷰에서 '규칙'을 어떻게 강제하시나요? 저는 이번에 타입과 모듈 경계로 강제해봤는데, 실무에서는 린트 룰이나 CI 체크를 어떻게 쓰시는지 궁금합니다."
5. "신입이 들어오면 처음 3개월 동안 주로 어떤 일을 맡게 되나요?"

### 마무리 멘트

> "이 프로젝트에서 제가 가장 신경 쓴 건 **'실수해도 안전한 구조를 만드는 것'** 이었습니다. 조직 격리도, 감사로그도, 실시간 emit 타이밍도 전부 '조심하기'가 아니라 '조심할 필요가 없게' 만드는 방향으로 설계했습니다.
>
> 동시에 **제가 만든 것의 한계도 알고 있습니다.** 커서 페이지네이션의 타이브레이커, 브랜드 타입의 우회 가능성, 수평 확장 미경험 같은 것들을 검수에서 정리했고 우선순위를 매겨뒀습니다.
>
> 실무에서는 제가 모르는 규모와 제약을 만날 텐데, **판단의 근거를 남기고 검증 가능한 형태로 만드는 습관**은 그대로 쓸 수 있을 거라고 생각합니다."

---

## 16. 절대 하면 안 되는 답변

| 상황                     | ❌ 금지                      | ✅ 대신                                                                          |
| ------------------------ | ---------------------------- | -------------------------------------------------------------------------------- |
| "왜 RLS를 안 썼나요?"    | "RLS를 몰라서요"             | 비용 계산(풀러 뒤 세션 변수, 트랜잭션 강제, 정책 관리)을 대고 향후 과제로        |
| "테스트 충분한가요?"     | "135개나 됩니다"             | 무엇을 검증했는지 + **프론트 0개라는 공백**을 먼저 말하기                        |
| "이 설계 단점은?"        | "딱히 없습니다"              | 14장 목록에서 3개 정도를 우선순위와 함께                                         |
| 데모 계정 질문           | "포폴이라 편의상 넣었습니다" | 우회 범위(리터럴 1건, 로그인 스키마만)와 **비밀번호는 절대 우회 안 함**을 정확히 |
| 카운터 버그              | "간단한 실수였습니다"        | **왜 테스트가 못 잡았는지**(자기 전이 미검증)까지 말하기 — 그게 배운 것          |
| "socket.io = WebSocket?" | "네"                         | "자체 프로토콜이고 폴백이 있습니다. 이 프로젝트는 폴백을 껐습니다"               |
| 성능 질문                | "빠릅니다"                   | 측정한 것만 말하기(쿼리 수 고정, 인덱스 매핑). 안 잰 건 "안 재봤습니다"          |
| localStorage 지적        | "요즘 다들 그렇게 합니다"    | XSS 취약을 인정 + 배포 토폴로지 근거 + 완화책 + **같은 도메인이면 쿠키 1순위**   |
| 문서 많다는 지적         | "꼼꼼한 편이라서요"          | 시간을 줄인 근거 3개 + **문서-코드 불일치 3건이 있다는 한계**도 함께             |
| 리전 장애                | "Render가 문제였습니다"      | 진단 절차(상태 페이지) + 판단(기다림 vs 우회)의 근거를 말하기                    |
| 모르는 질문              | 아는 척하고 추측             | "그건 안 해봤습니다. 다만 제 이해로는 ~인데, 맞는지 확인이 필요합니다"           |

---

> **INTERVIEW 끝.** 개념이 흔들리면 [STUDY.md](./STUDY.md) · [STUDY-2.md](./STUDY-2.md)로 돌아간다.
