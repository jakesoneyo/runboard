# SPEC — Runboard

> QA팀용 테스트 케이스·실행 관리 SaaS. 여러 조직이 완전히 격리된 데이터로 함께 쓰는 멀티테넌트 서비스.
> **유형**: 실시간(WebSocket) + 인증/보안 고도화(RBAC + 감사로그) ｜ **난이도**: L(2~3일, 쇼케이스)

용어는 [UBIQUITOUS_LANGUAGE.md](./UBIQUITOUS_LANGUAGE.md)를 따른다. 이 문서에 나오는 굵은 용어는 전부 거기 정의된 표준어다.

## 0. 이름

- 저장소·폴더명: `runboard` (확정, 리네이밍은 사람이 나중에 판단)
- 제품 표기: **Runboard** — "테스트 **Run**을 보드에서 같이 돌린다"는 뜻이 그대로 읽혀서 도메인과 잘 붙는다.
- 대안 후보(참고용): `TestRunboard`(중복 음절로 길다), `QA Runboard`(검색성은 좋으나 제품명에 조직명 성격이 섞임). **현행 유지 추천.**

---

## 1. 문제 정의

소규모 QA팀의 현실:

- 테스트 케이스는 스프레드시트에 있고, 실행 결과는 시트 복사본을 회차마다 새로 뜬다 → 어느 회차의 어느 버전을 돌았는지 나중에 알 수 없다.
- 3명이 같은 시트를 동시에 채우면 셀이 덮이거나, 누가 어디까지 했는지 몰라 같은 케이스를 두 번 돈다.
- FAIL을 발견하면 이슈 트래커에 재현 스텝을 손으로 다시 옮겨 적는다.
- 외주·계약직이 섞여 있어 "이 사람에게 다른 고객사 데이터는 절대 안 보여야 한다"가 필수인데, 시트 공유 권한으로는 보장이 안 된다.
- 사고가 나면 "누가 이 케이스 예상결과를 언제 바꿨나"를 되짚을 방법이 없다.

Runboard는 이 다섯 가지를 정면으로 푼다: **조직 단위 완전 격리 + 역할 기반 접근제어 + 실행 시점 스냅샷 + 동시 실행 실시간 동기화 + 감사로그**.

### 왜 이 프로젝트를 포트폴리오로 만드는가 (기술적 의도)

| 이 도메인이 자연스럽게 요구하는 것     | 증명하는 백엔드 역량                              |
| -------------------------------------- | ------------------------------------------------- |
| 여러 조직이 같은 테이블을 공유         | 멀티테넌시 격리를 **여러 계층에서 강제**하는 설계 |
| 같은 사람이 조직마다 다른 **Role**     | 토큰 클레임에 조직을 넣지 않는 RBAC 컨텍스트 설계 |
| 여러 명이 한 **TestRun**을 동시에 수행 | WebSocket 룸·권한·브로드캐스트 타이밍(커밋 후)    |
| 규제/추적 요구                         | 트랜잭션 내부 감사로그, 불변 기록                 |
| 통과율 추이 대시보드                   | 집계 쿼리·비정규화 카운터로 N+1 회피              |

---

## 2. 사용자와 역할

| Role        | 대표 인물         | 할 수 있는 일                                                                                                     |
| ----------- | ----------------- | ----------------------------------------------------------------------------------------------------------------- |
| **ADMIN**   | QA 매니저         | 조직 설정, 멤버 초대·**Role** 변경·제거, **AuditLog** 조회 + QA_LEAD의 모든 권한                                  |
| **QA_LEAD** | 테스트 리드       | **TestSuite/TestCase** 생성·수정·삭제, **TestRun** 생성·배정·종료, 모든 **RunCase** 기록, **BugReport** 상태 변경 |
| **TESTER**  | 테스터(외주 포함) | 자신이 **Assignee**인 **TestRun**의 **RunCase** 기록, **BugReport** 생성, 읽기                                    |
| **VIEWER**  | 개발자/PM         | 조직 내 모든 자산 읽기 전용(**AuditLog** 제외)                                                                    |

---

## 3. 핵심 기능 (범위 확정)

### F1. 멀티테넌시 — 조직 단위 완전 격리

- 한 **User**는 여러 **Organization**에 **Membership**을 가질 수 있고, 조직마다 **Role**이 다르다.
- 모든 리소스 URL은 `/api/orgs/:orgId/...` 로 조직이 명시된다. 요청자는 그 조직의 **Member**여야 하며, 아니면 **404**(403이 아니라 404 — 조직/리소스 존재 자체를 노출하지 않는다).
- 조직 경계는 3계층으로 강제한다(상세: ARCHITECTURE.md): 라우팅/가드 → Prisma Client Extension 자동 주입·검증 → DB 복합 FK 제약.
- 프론트에 조직 전환기(Organization Switcher)를 둔다. 전환 시 서버 상태 캐시는 조직 키로 분리된다.
- **성공 기준**: "다른 조직 데이터가 절대 안 샌다"를 Testcontainers 통합테스트로 리소스 유형별 전수 증명(PLAN.md T-그룹).

### F2. 테스트 스위트/케이스 CRUD

- **TestSuite**: 이름, 설명, 부모 스위트(최대 3단계 트리), 조직 스코프.
- **TestCase**: 제목, 사전조건, **Step** 배열(순서·행동), **Expected Result**, **Priority**, 소속 스위트.
- 스위트 삭제 시 하위 스위트·케이스 cascade. 단, 이미 만들어진 **RunCase** 스냅샷은 남는다(과거 실행 기록 보존).
- 쓰기 권한: QA_LEAD 이상.

### F3. 테스트 실행(TestRun) + 실시간 동기화 ← 실시간 유형의 핵심 증거

- QA_LEAD가 스위트 또는 케이스를 골라 **TestRun** 생성 → 선택된 **TestCase**들이 **RunCase**로 **스냅샷 복사**된다(제목·스텝·예상결과·우선순위). 이후 원본 케이스가 바뀌어도 실행 기록은 불변.
- **Assignee**(수행자) 다수 배정. TESTER는 배정된 **TestRun**만 기록 가능.
- 기록: `PENDING → PASS | FAIL | BLOCKED | SKIPPED` + 코멘트.
- **동시 수행 실시간 반영**: 한 명이 기록하면 같은 **TestRun** 룸의 모든 **Participant** 화면에 즉시 반영된다.
  - `run:case.recorded` — 어느 **RunCase**가 누구에 의해 어떤 **Result**로 바뀌었는지
  - `run:progress.updated` — 진행률/통과율 카운터
  - `run:presence.updated` — 지금 이 실행 화면을 보고 있는 사람 목록
  - `run:status.changed` — 실행 시작/종료
- 동시 기록 충돌 정책: **마지막 기록이 이긴다(LWW)**. 대신 이벤트에 `recordedBy`·`recordedAt`이 실려 "방금 누가 덮었는지"가 화면에 보인다. (낙관적 잠금은 비범위 — 아래 4장)
- 쓰기 경로는 **REST만** 사용하고 WebSocket은 브로드캐스트 전용(단일 검증·인가·트랜잭션·감사 경로 유지). 근거는 ARCHITECTURE.md.

### F4. 버그 리포트 연동

- FAIL로 기록된 **RunCase**에서 "버그 만들기" → 제목/설명/재현 스텝이 **RunCase 스냅샷**에서 자동 초안 생성(사용자가 수정 후 저장).
- 필드: 제목, 설명, 재현 스텝, **Severity**, **BugStatus**(OPEN/IN_PROGRESS/RESOLVED/WONTFIX), 파생된 **RunCase** 링크, 담당자(선택).
- 상태 변경은 QA_LEAD 이상, 생성은 TESTER 이상.

### F5. RBAC (조직 스코프) ← 보안 유형의 핵심 증거 (1/2)

- **Role**은 토큰이 아니라 요청 시점의 **Membership** 조회로 결정된다 → 권한 강등/추방이 즉시 반영(토큰 만료를 기다리지 않는다).
- 엔드포인트별 요구 **Role**을 데코레이터(`@RequireRole(...)`)로 선언하고 Swagger 문서에 자동 노출.
- TESTER의 "배정된 실행만" 규칙은 Role만으로 부족하므로 리소스 수준 인가(배정 여부 확인)를 별도 가드로 처리.

### F6. 감사로그 ← 보안 유형의 핵심 증거 (2/2)

- 기록 대상 **Action**: 케이스 생성·수정·삭제, 스위트 삭제, 실행 생성·종료, **RunCase** 결과 변경, 버그 생성·상태 변경, 멤버 초대·**Role** 변경·제거, 로그인 실패/성공, 리프레시 토큰 재사용 탐지.
- 도메인 트랜잭션과 **같은 트랜잭션 안에서** 기록한다(비즈니스 변경은 성공했는데 로그만 빠지는 상황을 원천 차단).
- 조회는 ADMIN 전용, 조직 스코프 + 필터(행위자/액션/대상/기간) + 커서 페이지네이션.
- 불변: 수정·삭제 API 없음.

### F7. 대시보드

- 최근 **TestRun** 목록(진행률·통과율·수행자)
- 통과율 추이 라인차트(최근 N개 실행 or 최근 30일) — Recharts
- **Result** 분포 바/도넛차트, 열린 **BugReport** 수(Severity별)
- 모든 수치는 조직 스코프. 비정규화 카운터 + `groupBy` 집계로 N+1 없이 계산.

### F8. 데모 로그인 (워크스페이스 표준 필수)

- 계정 `admin` / `admin`, 샘플 조직 1개에 **ADMIN**으로 시드.
- 샘플 데이터: 스위트 3개(중첩 포함), 케이스 8~12개, 완료된 **TestRun** 2건 + 진행 중 1건, **BugReport** 3건(상태 각각 다름), **AuditLog** 다수 → 로그인 직후 빈 화면이 없다.
- 2번째 조직(멤버십만 VIEWER)도 시드해서 **조직 전환기와 역할 차이**를 데모에서 바로 보여준다.
- 로그인 스키마에서 `email === 'admin'` **리터럴 하나만** 이메일 형식 검증 우회. 회원가입·프로필 수정에는 미적용.
- 비밀번호는 정상 bcrypt 비교. 인증 우회 엔드포인트 없음.
- 로그인 화면 고정 문구: 버튼 `회원가입 없이 둘러보기`, 보조 설명 `회원가입 없이 체험해 볼 수 있습니다.`
- 시드는 `scripts/seed-demo.ts` 1회성, 이미 있으면 스킵(idempotent).

---

## 4. 비범위 (Out of Scope) — 잘라낸 것과 이유

L티어 2~3일 안에 위 8개를 **깊게** 끝내기 위해 아래는 명시적으로 제외한다. README/면접에서 "왜 안 했나"를 이 표로 답한다.

| 제외 항목                          | 이유                                                                                                                                |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 파일/스크린샷 첨부                 | 오브젝트 스토리지·서명 URL·용량 정책이 붙어 하루가 통째로 나간다. 실시간·RBAC 증명에 기여 0.                                        |
| 실시간 채팅/코멘트 스레드          | 이미 실행 결과 브로드캐스트로 실시간 역량은 증명된다. 채팅은 같은 기술의 반복.                                                      |
| 이메일 발송(초대 메일·비번 재설정) | SMTP 계정·전송 실패 처리·템플릿 = 인프라 작업. 초대는 **초대 링크 토큰 복사** 방식으로 대체.                                        |
| 소셜 로그인(OAuth/SSO), 2FA        | Passport-JWT + 리프레시 회전 + 재사용 탐지로 인증 깊이는 충분. OAuth는 다른 프로젝트 소재.                                          |
| 낙관적 잠금(버전 충돌 UI)          | 동시 기록은 LWW + "누가 덮었는지 실시간 표시"로 실사용 문제를 해결. 충돌 해소 UX는 과설계.                                          |
| 케이스 버전 히스토리 diff 뷰어     | 실행 스냅샷 + 감사로그 metadata의 before/after로 추적 가능. 전용 버전 테이블·UI는 과설계.                                           |
| JUnit/XML 자동화 결과 임포트       | 파서·매핑 규칙이 별도 도메인. 수동 실행 흐름이 실시간 데모의 본질.                                                                  |
| Jira/GitHub Issues 연동            | 외부 API 연동은 **다른 유형 태그**라 이번 프로젝트 정체성을 흐린다.                                                                 |
| 결제·플랜·조직 사용량 제한         | SaaS 과금은 도메인 밖.                                                                                                              |
| 알림 센터/이메일 알림              | 실시간 이벤트로 화면에서 이미 보인다.                                                                                               |
| Postgres RLS(행 수준 보안)         | Neon 풀러 + Prisma 조합에서 세션 변수 관리 비용이 크다. 애플리케이션 3계층 강제로 대체(근거는 ARCHITECTURE.md). 향후 과제로 문서화. |
| WebSocket 수평 확장(Redis adapter) | Render 무료 인스턴스 1대 기준. 확장 시 필요한 변경점만 문서화.                                                                      |
| i18n, 모바일 앱, 다크모드 토글     | 포트폴리오 평가 축에 기여하지 않음(UI는 한국어 단일, 반응형만 보장).                                                                |
| CSV 임포트/엑스포트                | 시간 대비 임팩트 낮음. 시드 데이터로 충분히 채워진 화면을 보여준다.                                                                 |

---

## 5. 유저 스토리 & 수용 기준

| #     | 스토리                                                            | 수용 기준 (테스트로 검증)                                                                          |
| ----- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| US-1  | 면접관으로서, 가입 없이 완성된 화면을 보고 싶다                   | 데모 버튼 1클릭 → 대시보드에 차트·실행·버그가 채워진 상태로 진입                                   |
| US-2  | ADMIN으로서, 멤버를 초대하고 역할을 바꾸고 싶다                   | 초대 링크 생성 → 다른 계정이 수락 → **Membership** 생성, `MEMBER_ROLE_CHANGED` **AuditLog** 기록   |
| US-3  | 외주 TESTER로서, 우리 조직 데이터만 보여야 한다                   | 타 조직 orgId로 모든 리소스 접근 시 404, 목록 응답에 타 조직 레코드 0건 (전수 통합테스트)          |
| US-4  | QA_LEAD로서, 스위트를 골라 실행을 만들고 테스터에게 배정하고 싶다 | **TestRun** 생성 시 **RunCase** 스냅샷 N건 생성, 이후 원본 케이스 수정해도 스냅샷 불변             |
| US-5  | TESTER로서, 동료와 같은 실행을 나눠 돌고 싶다                     | 브라우저 2개에서 같은 실행 진입 → 한쪽 기록이 다른 쪽에 500ms 이내 반영, 진행률·접속자 목록 동기화 |
| US-6  | TESTER로서, 배정 안 된 실행은 기록할 수 없어야 한다               | 미배정 TESTER의 기록 요청 → 403, QA_LEAD는 배정 없이도 200                                         |
| US-7  | TESTER로서, FAIL 난 케이스에서 버그를 바로 만들고 싶다            | 초안에 스냅샷 제목·스텝이 채워짐, 저장 시 **RunCase**와 링크됨                                     |
| US-8  | ADMIN으로서, 누가 무엇을 언제 바꿨는지 추적하고 싶다              | 케이스 수정 → 감사로그에 before/after metadata, ADMIN 아닌 역할은 403                              |
| US-9  | PM(VIEWER)으로서, 통과율 추이를 보고 싶다                         | 대시보드 차트가 조직 스코프 집계값으로 렌더, 쓰기 API는 전부 403                                   |
| US-10 | 사용자로서, 로그인 상태가 오래 유지되되 안전해야 한다             | access 만료 시 refresh 자동 회전, 폐기된 refresh 재사용 시 계열 전체 폐기 + 감사로그               |

---

## 6. 성공 기준 (측정 가능)

- **격리**: 크로스 테넌트 접근 통합테스트 전수 통과(리소스 유형 × 읽기/쓰기 조합). 실패 0건.
- **RBAC**: Role × 주요 엔드포인트 매트릭스 테스트 통과.
- **실시간**: 로컬/스테이징에서 기록 → 타 클라이언트 반영 p95 < 500ms, 소켓 재연결 시 상태 재동기화.
- **테스트**: 도메인 서비스 라인 커버리지 80%+, Testcontainers 통합테스트 그린, CI 필수 통과.
- **문서**: 모든 엔드포인트 Swagger 노출(요청/응답 DTO 포함), README에 mermaid 아키텍처 + 데모 GIF.
- **배포**: 프론트 Vercel 라이브, 백엔드 Render(Docker) 라이브, `/health` 200, 콜드스타트 후 첫 화면 10초 이내.
- **운영성**: 마이그레이션은 `prisma migrate deploy`로 컨테이너 기동 시 자동 적용, 시드는 idempotent.

## 7. 리스크

| 리스크                                        | 대응                                                                        |
| --------------------------------------------- | --------------------------------------------------------------------------- |
| Render 무료 인스턴스 슬립 → 데모 첫 진입 느림 | README에 안내 + 프론트에 첫 요청 로딩 상태 명시, `/health` 워밍업 호출      |
| Neon 풀러 + Prisma 커넥션 한계                | `connection_limit` 축소, `DIRECT_URL`은 마이그레이션 전용                   |
| WebSocket이 프리티어에서 끊김                 | 자동 재연결 + 재연결 시 REST로 전체 상태 재조회(이벤트 유실 복구 경로 확보) |
| 복합 FK 제약을 Prisma가 표현 못 하는 경우     | 마이그레이션 SQL에 제약 직접 추가하는 fallback 명시(DATA-MODEL.md)          |
| 범위 초과                                     | 4장 비범위 표를 게이트로 사용. 새 아이디어는 README "향후 과제"로만 적는다. |
